# SlideHuddle — Progress Tracker

*Lives at `docs/PROGRESS-TRACKER.md`. The single source of truth for where we are on the master plan (see "Gap Analysis & Master Build Plan", June 2026). Claude Code updates this file at the end of every session; the founder updates the items marked 👤 and the metrics.*

---

## Instructions for Claude Code (read before every update)

You are updating this tracker at the **end of a working session**. Follow these rules exactly:

1. **Append, never rewrite.** Add a new entry at the **top** of the Session Log. Never delete or edit past entries or past status history.
2. **Status changes need evidence.** Only change an item's status if this session's work justifies it, and put the evidence in the item's Evidence column (file/function, migration name, or commit). An item is ✅ only when it works end-to-end and you've stated how it was verified — "code written" is 🔵, not ✅.
3. **Statuses:** ⬜ not started · 🔵 in progress · ✅ done · ⏸ blocked (say why in Blockers) · ❌ dropped (say why in the session entry).
4. **Update the Dashboard counts** (done/total per phase) and the "Current focus" line to match reality.
5. **New work goes to the Parking Lot**, never silently into a phase. If a session uncovers a bug, debt, or a new idea, add it there with a one-line description and your proposed phase. The founder promotes parking-lot items into phases, not you.
6. **Gates are sacred.** Never mark a gate passed — only the founder does that (👤). You may write "gate evidence ready" in the session entry.
7. **Don't break the format.** Keep the tables' columns as they are; keep item IDs stable; dates as YYYY-MM-DD.
8. **After a gate passes**, compress that phase's session-log entries into a single "Phase N summary" block (preserving dates and key evidence) to keep this file readable.
9. If the session touched the database schema, security-relevant code, or the MCP tool surface, say so explicitly in the session entry under "Flags".

---

## Dashboard

| | |
|---|---|
| **Current phase** | Phase 1 (product track) — Gate G0 not yet passed; founder still owns P0.7–P0.9 in parallel |
| **Current focus** | **PDF export (P0.5) built 2026-07-03 🔵 — awaiting founder fidelity check (see session log).** Also: **Feed↔deck spectrum — Slices 1+2+3 ✅ built (2026-07-03), on branch `spectrum-slices-1-2`** — the deck + the conversation feed coexist on one resizable screen behind **`?view=spectrum`** (URL-only; the default and the partner feed test are unchanged). Divider → deck/split/feed (`?mode=`); rail ↔ full feed; version navigation inside the spectrum; comments panel folds in feed mode (available in deck/split); "+" insert at both fidelities; provider-neutral "AI" copy (classic keeps "Claude"); live feed (Realtime merge + token-refresh hardening); slide-clustered rounds; owner curation on feed cards. **Slice 3 (4 founder rounds):** the far-left top-anchored **Huddlers filter stack** — YOU on top (account identity: person icon + green dot), the AI's model mark (32px + purple version count, clickable filter), collaborators (owner = purple star, no ring), a dashed "+" invite seat (copies the share link, same toast, on-screen clamped); actionable-only contribution counts; filter HIDES non-matching cards, empty state for zero-contribution people, scroll-to-current-round on clear; soft avatar restyle everywhere + I1 fixed. **All committed + pushed on `spectrum-slices-1-2`** — awaiting founder end-to-end test, then merge to `main`. Later: presence dot, in-session comment notification, mobile, text-selection comments, `SPECTRUM_PARTNER_EMAILS`. | Converges the duplicated renderings: the feed column was extracted into a shared `FeedStream` (one copy), reused by `?view=feed` + the spectrum; classic untouched; no auth/RLS/service-role/MCP change. tsc+lint+preview green; **uncommitted**. Slices 2 (fold the comments panel) + 3 (avatar filter) + copy unification pending (see Parking Lot). This is the informal **"Phase-7" converge direction, not a formal milestone**. — **P1.2 🔵 (2026-06-17)** — read-only conversation feed (version-spine backbone + avatars + AI provenance) as an alternative landing for partners, flag-gated via `FEED_PARTNER_EMAILS` + `?view=feed`/`?view=deck`, both landings instrumented with PostHog (no-ops without a key). Latest: **"settled history" greyscale (handover Item A)** — past-round version messages + addressed/dismissed items desaturate so unaddressed feedback pops; hover/selection restores colour. The prior feed work is **committed + pushed to `main`**; today's greyscale (Item A) is tsc/lint/preview-green but **uncommitted**. Awaiting founder partner end-to-end test + a PostHog key to light up the Gate-G1 measurement. See `docs/P1.2-HANDOVER.md`. — P1.1 🔵 — **Stages B + C + D done** (B approved by founder 2026-06-14). D: anonymous recipients now see a count-only **"N reviewing"** chip (not "Huddlers"/"Shared deck"); deck-title z-index confirmed already clean (title lives inside the brand pill); inset/Floating-Canvas styling already applies to the recipient view. **Stage E (mobile) DEFERRED by founder** until the base desktop experience is complete. Today's P1.1 desktop work (Stages B–D + fixes) **committed + pushed to `main` 2026-06-14** (deploys via Vercel; CI green). **Stage F (rollout)** is effectively satisfied by default-for-all + `?view=classic` fallback — to close it: a founder prod smoke-check; per-account/test-group targeting parked → **Phase 2** (see Parking Lot). Stage E (mobile) parked. |
| **Phase progress** | P0: 5/9 · P1: 0/4 (P1.1 + P1.2 in progress) · P2: 0/6 · P3: 0/6 · P4: 0/5 · P5: 0/6 |
| **Gates passed** | none |
| **Open blockers** | 0 |
| **Last session** | 2026-07-14 — cleanups: test suite 54/54 green (chrome assertions moved to `?view=classic`), `feedback_curated` event + 5th dashboard insight, `.wip-backup/` deleted, and a real bug fixed en route (floating viewer "+"-insert infinite recursion from the undo commit). Prior day: P0.2 closed ✅ (PostHog cohort + G1 dashboard). |

### Key metrics (👤 founder-updated; "—" until analytics is live)

| Metric | Target | Current | As of |
|---|---|---|---|
| Weekly completed review rounds (north star) | growing | — | |
| Activation (first round, ≥2 people, ≤7 days) | ≥30% | — | |
| Second huddle within 30 days | ≥40% | — | |
| Viral coefficient k | ≥0.2 by M6 | — | |
| Free → paid | 4%+ | — | |
| Founding partners signed 👤 | 5–10 | 0 | |
| MRR 👤 | per scenario | $0 | |

---

## Phase 0 — Truth, safety, measurement (target: weeks 1–3)

| ID | Item | Size | Status | Evidence | Date |
|---|---|---|---|---|---|
| P0.1 | Run `verify-rls.sql` in production; confirm RLS live on all 7 tables | S | ✅ | Greg ran verify-rls.sql in prod Supabase 2026-06-13: all 7 tables `rls_enabled=true`; anon-policy check returned 0 rows (no policy exposed to logged-out role) | 2026-06-13 |
| P0.2 | Analytics from zero: install + named event schema + funnel/channel attribution + dashboards | S–M | ✅ | Gate-G1 event set wired per `docs/G1-MEASUREMENT.md` §4 (identity+`is_partner` super-prop, `feedback_added`{surface,version,role}, `send_to_ai_clicked`, server-side `version_published`; existing events normalised to snake_case). PostHog key live local+prod (EU). **2026-07-13: §6 dashboards + cohort built via the PostHog MCP** — "Design partners" cohort (id 180915; self-maintaining: `is_partner=true` minus founder/test emails) + "G1 — Phase 1 validation" dashboard (id 816687; 4 insights, all cohort-filtered, funnel aggregated per deck). Verified: all 5 G1 events confirmed flowing in the project; every query shape executed green; breakdown + funnel validated against real founder-test data (funnel showed 1 full loop conversion). Dashboard reads zero **by design** until a real partner is in `FEED_PARTNER_EMAILS`. | 2026-07-13 |
| P0.3 | Fix: extension update path calls `clearAddressedFeedback` (parity with MCP) | S | ✅ | api/slides/route.ts: import + best-effort call after updateDeck (mirrors mcp/route.ts:430) + `resolvedFeedbackCount` in response. Verified e2e: extended test-loop.mjs seeds a stub+flag, runs the token-authed update, asserts both `resolved_at` set + count=2 — all pass (53/54; the 1 fail is unrelated test-drift, see Parking Lot). Code not yet committed | 2026-06-13 |
| P0.4 | Fix: orphan-deck recipients get a sign-in/claim nudge instead of a silent comment block (instrumented) | S | ✅ | Viewer detects orphan decks (deck.user_id null, post-claim) and shows a clear nudge in the comments panel instead of a silently-failing composer; canComment/canInsert/canFlag gated off for orphans (page.tsx + SlideViewer.tsx + CommentsPanel.tsx). tsc+eslint clean; verified via browser preview (anon viewer on an orphan deck sees the nudge — screenshot). **Instrumentation deferred with P0.2 (analytics).** | 2026-06-13 |
| P0.5 | PDF export — **revived by founder 2026-07-03** (was deferred 2026-06-13; deferral rationale preserved in the session log) | M | 🔵 | Built 2026-07-03: Export icon in the floating viewer's top-left collapsible (beside the version chip, founder placement call) → `/viewer/print?id&v` renders every slide of the viewed version one-per-page through the same sandboxed render path, then auto-opens the print dialog ("Save as PDF"); version-aware label "Export v{N} as PDF"; PDF filename "{title} — v{N}". Verified in browser preview: sheets at natural size, version lens correct (v1→2 sheets vs v3→3 on the smoke deck), error state, sandbox unchanged, tsc+lint clean. **Not ✅** until the founder saves a real PDF and confirms fidelity end-to-end. PPTX export still out of scope. | 2026-07-03 |
| P0.6 | CI baseline: lint + `test-loop.mjs` on push to main | S | ✅ | `.github/workflows/ci.yml` runs `npm ci` → `npm run lint` → `npm run typecheck` (Node 20) on push-to-main + PRs; added `typecheck` script to web/package.json. No DB/secrets. Pushed 2026-06-13 → **first CI run on GitHub = completed/success** (verified via GitHub API); locally demonstrated a deliberate type error makes it fail red. test-loop.mjs deliberately EXCLUDED (service-role key + live DB) → wire later vs a dedicated test Supabase project. | 2026-06-13 |
| P0.7 | 👤 Trademark searches (UK IPO + USPTO, incl. Slack-"Huddles" question) + name go/no-go | S | ⬜ | | |
| P0.8 | 👤 UK Ltd incorporated · Stripe account · ICO registration | S | ⬜ | | |
| P0.9 | 👤 Real-user test: 2–3 outsiders run the full loop (post P0.2–P0.5) | — | ⬜ | | |

**Gate G0** (👤): loop completable end-to-end (PDF export deprioritized 2026-06-13 — no longer required, see P0.5) · events flowing · RLS verified · name decided · outside users observed. **Status: not passed.**

## Phase 1 — The feed, on the floating viewer (target: weeks 3–6)

| ID | Item | Size | Status | Evidence | Date |
|---|---|---|---|---|---|
| P1.1 | Floating viewer completion: version polling · flag-creation UI · mobile layout pass · zoom (implement or remove) | M | 🔵 | **Stage 2 (occlusion/inset) done + zoom removed.** FloatingViewer.tsx: open strip/comments now shrink+shift the slide into the safe area beside the panel (design-system §3.3) instead of covering it; inert zoom placeholder removed. Verified in preview (1280×800): strip-open slide.left 0→213, clean 12px gap to strip (no overlap); tsc+eslint clean. **Stage 1 (cosmetic) done** — but the earlier amber decision was reversed by the founder mid-session: top-right action cluster now uses hierarchy not colour (Share filled-purple = only filled button; Send to AI reverted to purple-outline split; Comments restyled to a bare teal icon+count). Recipient-view z-index (#3) and brand-pill icon (#4) confirmed already clean. New colour rule (purple = actions you take incl. Send to AI; amber = the AI's own voice only) applied to `design-system.md` §2.2/§3.2/§5/§6/§10 + header. **Stage B persistence (§4.1) done**: rail **sliver** on the left edge (always visible, 14px, teal comment-activity dots = "the team's fingerprints") expands to the full strip on hover/tap/`T`; **counter now always-on** (no longer fades); `T` toggles the rail; arrow-key nav guarded against firing while typing. With the inset + cluster work, **Stage B (deck-viewer redesign) is complete** — pending founder visual approval. Remaining: Stage C version-poll/flag-UI, Stage D recipient view, Stage E mobile, Stage F rollout. tsc+eslint clean. Not committed | 2026-06-14 |
| P1.2 | Read-only feed view: comments + stubs + flags + version events as one stream; deck demoted to peek; flag-gated, default-on for partners | M | 🔵 | Built the read-only "Huddle feed" landing (design-system §6.2): pure `feed-items.ts` composes version events + all-version comments + stubs + flags into one oldest-first stream; new `DeckFeed.tsx` renders it (chat-style cards, day dividers, catch-up ribbon, dismissed=struck, "Slide N · vN" peek-nav chips) with the deck demoted to a right **peek** (scaled iframe via the shared `parseDeck`/`buildSrcdoc`) + **Open deck** (`?view=deck`). Reuses `DeckVersionNav`/`HuddleAvatars`/`AvatarMenu` + chips extracted to shared `HuddleChips.tsx`. Gating in `page.tsx`: `FEED_PARTNER_EMAILS` allowlist + `?view=feed`/`?view=deck` override; new read-only `getAllCommentsForDeck`. **Instrumented** (PostHog seam, no-ops without key): `deck_landing_viewed{view:feed\|deck}` on BOTH landings + `feedback_added{kind}` in the 3 deck hooks. Verified: tsc+eslint clean, pure-logic node test (ordering/kinds), browser preview of the real `DeckFeed` (all 4 card kinds in order, peek scales 288/1280, Open deck→view=deck, events fire, no console errors) + deck viewer unregressed. **Not committed.** Needs founder end-to-end test (partner email → feed). **Update 2026-06-17:** committed + pushed to `main`; since added — version-spine backbone, avatar system, AI provenance (`deck_versions.source`), and **"settled history" greyscale (handover Item A)** that greys past-round version messages + addressed/dismissed items while unaddressed feedback stays in colour. Still 🔵 pending founder partner test + a PostHog key (Gate-G1 evidence); see `docs/P1.2-HANDOVER.md`. | 2026-06-16 |
| P1.3 | Viral loop v0: viewer badge · recipient post-comment CTA · claim-flow funnel events · export footer option | S | ⬜ | | |
| P1.4 | 👤 Design-partner recruitment running (15–20 contacts/week; demos on the feed) | — | ⬜ | | |

**Gate G1 / Assumption 1** (👤): ≥half of partner feedback in-product by round 2; feed preferred or equal as landing view. **Status: not passed.**

## Phase 2 — Identity, membership, money (target: weeks 6–10)

| ID | Item | Size | Status | Evidence | Date |
|---|---|---|---|---|---|
| P2.1 | Profiles: display name + avatar, resolved server-side (also fixes realtime "a teammate") | S | ⬜ | | |
| P2.2 | Email provider (transactional) + invite-by-email + pending invites + member list | M | ⬜ | | |
| P2.3 | Role enforcement in RLS + named/invited guest (client) role | M | ⬜ | | |
| P2.4 | Workspaces entity: members, shared deck ownership, admin | M | ⬜ | | |
| P2.5 | Billing: Stripe Billing + Tax, entitlements (active-huddle counter, feature flags), all four plans wired | M | ⬜ | | |
| P2.6 | Trust & legal: ToS · privacy · trust page · data export/delete · DPA template | S–M | ⬜ | | |

**Gate G2 / Assumption 2** (👤): ≥5 founding partners committed with a card; 3+ named people assembled by invite; first charge processed. **Status: not passed.**

## Phase 3 — Conversation core (target: weeks 10–16)

| ID | Item | Size | Status | Evidence | Date |
|---|---|---|---|---|---|
| P3.1 | Messages evolution: `kind` column · optional `slide_index` · activate `parent_id` threading · stubs/flags as feed kinds | M | ⬜ | | |
| P3.2 | Slide quoting: version-pinned quote payload (+ `element_id` anchors) · thumbnail cards · both quote gestures · tap-quote-to-peek | M | ⬜ | | |
| P3.3 | Decisions: promote-to-decision · `approved_at` · owner approval queue · decision log view | S–M | ⬜ | | |
| P3.4 | Presence channel ("here now") on the huddle avatars | S | ⬜ | | |
| P3.5 | Conversation completeness: reactions · comments-on-stubs · author self-edit UI | S | ⬜ | | |
| P3.6 | 👤 Client guest mode live with 2 partner agencies' real clients | — | ⬜ | | |

**Gates G3 / Assumptions 3–4** (👤): ≥40% second-huddle-in-30-days among partners; a real client completes a round unaided. **Status: not passed.**

## Phase 4 — AI as participant (target: weeks 14–18, overlaps P3)

| ID | Item | Size | Status | Evidence | Date |
|---|---|---|---|---|---|
| P4.1 | `get_feedback` returns approved decisions with thread context | S | ⬜ | | |
| P4.2 | `update_deck` posts a system feed message + AI change summary; resolves consumed decisions | S | ⬜ | | |
| P4.3 | Catch-up digest: AI summary pass on the arrival-activity mechanism | S–M | ⬜ | | |
| P4.4 | Optional `post_message` MCP tool | S | ⬜ | | |
| P4.5 | OAuth-focused security re-review of the changed tool surface | S | ⬜ | | |

**Gate G4** (👤): a full AI revision round legible in the feed without opening the deck. **Status: not passed.**

## Phase 5 — Reach, polish, launch (target: weeks 18–24)

| ID | Item | Size | Status | Evidence | Date |
|---|---|---|---|---|---|
| P5.1 | Notification system: mentions · decision alerts · version posts · daily digest | L | ⬜ | | |
| P5.2 | Onboarding: sample huddle + guided first round ("next client deck" wedge) | M | ⬜ | | |
| P5.3 | Search: Postgres FTS over messages/decisions + UI | S | ⬜ | | |
| P5.4 | Status / review deadline / sign-off | S | ⬜ | | |
| P5.5 | Studio branding + client-facing decision-log view | S–M | ⬜ | | |
| P5.6 | 👤 Launch wave: Product Hunt / Show HN · MCP directory · Chrome Web Store refresh · first comparison pages | — | ⬜ | | |

**Gate G5** (👤): activation ≥30% · k measured · free→paid trending to 4%+ · most participation via notifications. **Status: not passed.**

## Phase 6 — Post-validation bets (not scheduled)

Slack/Teams bridge · AI variants + voting · live huddle mode · mobile push · multi-AI via existing `search`/`fetch` aliases · client decision-log distribution · docs/content expansion (prepared pivot).

---

## Blockers (CC adds; founder clears)

| Opened | Item ID | What's blocked and why | Cleared |
|---|---|---|---|
| | | | |

## Parking Lot (new work discovered mid-journey — CC adds, founder promotes)

| Added | Description (one line) | Proposed phase | Promoted? |
|---|---|---|---|
| 2026-06-13 | Stale `test-loop.mjs` assertion: "chip still shows current version while viewing history" greps for literal "Version 2" on the `?v=1` page, but the viewer now shows the latest as short-form "v2" inside an older-version warning label (product is correct). Fix the assertion so the suite is green before P0.6 wires it into CI. | P0 (unblocks P0.6) | ✅ Done 2026-07-14 (suite 54/54 green; chrome assertions now probe `?view=classic` — see session log) |
| 2026-06-13 | Floating viewer (`?view=floating`, off by default) has the SAME orphan-deck dead-end P0.4 fixed in the current viewer — its comment path isn't orphan-aware. Port the orphan nudge when finishing the floating viewer. | P1 (with P1.1) | ✅ Done 2026-06-14 (P1.1 Stage C) |
| 2026-06-14 | **Per-account / test-group viewer targeting.** Today the floating viewer is default-for-ALL with `?view=classic` + the `FLOATING_VIEWER_DEFAULT` env kill switch as fallbacks — there's no way to put a *specific account* (or a named test group) on one viewer vs another. Lightest partial = make `?view=` sticky per browser (a cookie). A true per-account/admin toggle needs the **workspaces + profiles/per-user-flag** entities, so it naturally rides with those. Not needed while the only audience is the close design-partner group. | P2 (with workspaces/profiles, P2.1/P2.4) | |
| 2026-06-16 | **P0.2 partially seeded by P1.2.** PostHog client lib (`posthog-js`) + a vendor-agnostic seam (`lib/analytics.ts` `track`/`identifyUser`, no-ops without a key) + a root `PostHogProvider` + 3 wired events (`deck_landing_viewed`, `feed_open_deck`, `feedback_added`) now exist. Remaining for P0.2 to close: founder creates a PostHog project, sets `NEXT_PUBLIC_POSTHOG_KEY` (+ `NEXT_PUBLIC_POSTHOG_HOST` if EU) in `.env.local` **and Vercel**, then builds the funnel / channel-attribution dashboards. Until the key is set, analytics is inert. | P0 (P0.2) | |
| 2026-06-16 | **Feed partner gate is an env email allowlist** (`FEED_PARTNER_EMAILS`) — the lightest per-account gate without P2. A real per-account/admin toggle (and making it not require an env redeploy to add a partner) rides with the same **workspaces + profiles** work as the viewer-targeting item above. | P2 (with workspaces/profiles) | |
| 2026-06-16 | **Mobile feed layout (§6.6).** `DeckFeed` is desktop-first: the deck-peek panel hides below `lg` (the feed column still reads fine on mobile, but there's no bottom-sheet peek). Fold into the same mobile pass as the deck viewer's parked Stage E (floating panels → bottom sheets). | P1/P2 (with mobile pass) | |
| 2026-06-23 | **Classic viewer emits `send_to_ai_clicked` with `deck_id:null`.** `SendToClaudeButton` is shared; the classic viewer renders it via `ThumbnailStrip` without the new `deckId`/`surface` props (they default to null/"deck"). Low impact — the floating viewer is the default — but classic-origin events lack a deck id. Pass `deckId`/`surface` from `ThumbnailStrip`, or just let it die with the classic viewer at the Phase-7 cutover. | P1 / P7 (classic retire) | |
| 2026-06-23 | **Optional G1 events not built** (`docs/G1-MEASUREMENT.md` §4): `feedback_curated` (owner edit/dismiss/restore — moat-health read) and `huddle_created` (funnel top). Add if/when those questions matter. | P1/P2 | `feedback_curated` ✅ Done 2026-07-14 (+ §6.5 dashboard insight); `huddle_created` still open |
| 2026-06-23 | **Email now stored in PostHog as a person property** (founder decision, overriding the "no emails in analytics" house rule) to enable the email-based "Design partners" cohort. Revisit when the privacy posture tightens — fold into **P2.6 trust & legal** (privacy policy / cookie consent) before any broader (non-partner) audience. | P2.6 (trust & legal) | |
| 2026-06-23 | **Extension-path `version_published` attribution.** When the extension `?update=` path saves a version on an ORPHAN deck (no owner), the event's `distinct_id` falls back to `deck:<id>` (not a person), so it won't join the partner cohort (still counts toward the raw north-star). The MCP path always attributes to the owner. Resolve naturally once decks always have an owner / Phase-2 identity. | P2 (identity) | |
| 2026-07-02 | **Feed↔deck spectrum — Slices 2–3 + later** (informal "Phase-7" converge direction, NOT a formal milestone). Slice 1 landed behind `?view=spectrum` (deck + feed on one resizable screen; shared `FeedStream` extracted from `DeckFeed`; version navigation preserved inside the spectrum). **Slice 2 ✅ 2026-07-02** (folding panel + "+" at both fidelities + provider-neutral copy; fix round same day: live feed via Realtime merge, slide-clustered rounds, owner curation on feed cards, chip sizing). **Slice 3 ✅ 2026-07-03** (Huddlers filter stack far-left + soft avatar restyle + contribution counts + I1 fix — founder-approved mock; the deck-mode rail-dot highlight variant was superseded by the founder's "stack hidden in deck mode" call). See session entries. **Later:** text-selection comments (needs an iframe-out anchoring strategy; `element_id` reserved); mobile (feed full-screen + slide as a tap-up sheet). Also: add a `SPECTRUM_PARTNER_EMAILS` default-on flag (mirroring `FEED_PARTNER_EMAILS`) once the surface proves out. | P1 (product) / P7 (converge) | |
| 2026-07-03 | **Presence system ("online now" green dot).** The Slice-3 avatar anatomy reserves a green top-right dot for "connected to the app right now", and offline-dimming — but nothing tracks live connections today (the Huddlers data is "who's involved", not "who's here"). Needs Supabase Realtime **Presence** channels (join/leave per deck), a privacy think (anonymous viewers must stay uncounted-by-identity), and then ~20 lines in `HuddleFilterStack`/`Avatar` to light the dot + dim offline. | P1/P2 (product) | ✅ Done 2026-07-05 (dot-only, founder call — no offline dimming; BEHAVIOURS G9) |
| 2026-07-02 | **In-session "new comment" notification.** The spectrum's feed now updates LIVE when a teammate comments (Realtime merge, J7), but there's no proactive nudge — a founder-requested "you got a comment" signal while you're in the deck (a toast, or a pulse on the Huddlers cluster). The cross-session case is covered by the arrival banner (B8) / ribbon (A8). Design the in-session signal so it doesn't interrupt reading (the floating chrome deliberately tucks away). | P1 (product) | ✅ Built 2026-07-04 (founder-promoted; BEHAVIOURS B12) |
| 2026-07-13 | **Remove founder emails from `FEED_PARTNER_EMAILS` when the first real partner is added.** Today the allowlist contains only the founder's two emails (needed for testing the feed). The PostHog cohort excludes him by email, but his events still carry `is_partner=true` — any insight filtering on the event property (rather than the cohort) would count him. When a real partner lands: add their email, remove the founder's, and use `?view=feed` for founder testing instead. | P1 (with first partner) | |
| 2026-07-05 | **Owner hard-delete of a collaborator's removal flag.** The flag-Delete link (C5) is RLS-flagger-only, so the owner can Dismiss but not delete someone else's flag. Add a `setFlagCurationAction`-style server action (or RLS policy) enforcing owner-or-flagger delete, then widen the Delete link's condition. Security-relevant (touches RLS/a server action on `slide_flags`). | P2/P3 (curation) | |

## Standing ops checklist (from the inventory's Uncertain list)

| Item | Status |
|---|---|
| RLS verified live in production (= P0.1) | ✅ 2026-06-13 |
| Applied-migrations record exists | ⬜ |
| Supabase auth config audited (redirect allowlist, templates) | ⬜ |
| Vercel env vars audited | ⬜ |
| Extension distribution channel decided/documented | ⬜ |
| Docs regenerated after last gate (TECHNICAL.md + re-run inventory prompt) | ⬜ |

---

## Session Log (newest first — CC appends here)

> **Template — copy for each session:**
>
> ### YYYY-MM-DD — (one-line session goal)
> - **Items touched:** P0.x 🔵→✅ (one line each: what was done, evidence)
> - **Files changed:** (paths only)
> - **Verified by:** (how you know it works — test run, manual check, screenshot)
> - **Flags:** schema change? security-relevant? MCP surface changed? (yes/no each)
> - **New parking-lot entries:** (or "none")
> - **Recommended next session:** (one line)

### 2026-07-14 — Small cleanups: test suite green, `feedback_curated` event, undo recursion bugfix
- **Items touched:** none change status — three founder-approved cleanups + one bug found en route.
- **(1) `test-loop.mjs` fully green (54/54)** — closes the 2026-06-13 parking-lot item, and wider than expected: the floating viewer (default since P1.1 Stage F) renders its version CHROME client-side, so ALL four chrome greps failed against SSR HTML, not just the stale one (empirically probed: default view SSRs slide content but no chip/warning). Fix: content assertions stay on the default view (they prove the version data path); the four chrome assertions now probe `?view=classic` (the only SSR'd chrome; commented to retire with classic at the Phase-7 cutover), with the stale literal corrected to classic's real copy ("The latest is v2").
- **(2) `feedback_curated` event (G1-MEASUREMENT §4 optional)** — fires on confirmed curation saves in all three hooks: dismiss/restore (comments, flags, stubs) + owner edit (comments) + stub field edit (requester-or-owner; `role` prop disambiguates). Undo inverse calls pass a new `{track:false}` through the widened curation signatures, mirroring the existing feedback_added convention, so undo cycles never inflate the metric. Also added the §6.5 "Curation usage" insight (id 4992023) to the G1 dashboard, cohort-filtered, broken down by action.
- **(3) Deleted `.wip-backup/`** — two 2026-06-10 snapshots (FloatingViewer/HuddleAvatars) superseded by ~20 commits.
- **(BUG FIX, found en route)** `FloatingViewer.insertStubU` called ITSELF instead of `insertStub` — infinite recursion: inserting a requested slide via "+" in the floating viewer would crash (stack overflow) since the undo integration commit (1ff042c). One-line fix. This means the "+"-insert path was broken in the 2026-07-05 batch — worth exercising in the founder feel-test.
- **Files changed:** `web/scripts/test-loop.mjs`, `useDeckComments.ts`, `useDeckFlags.ts`, `useDeckStubs.ts`, `FloatingViewer.tsx`, this tracker.
- **Verified by:** tsc + eslint clean; full suite 54/54 green (run twice, incl. after the analytics changes); anon viewer browser-checked (renders clean, no console errors). NOT verified live: the curated event firing (owner curation needs a signed-in owner session — magic-link auth blocks CC; Greg can watch for `[analytics] feedback_curated` in the dev console or PostHog Live Events) and the "+"-insert fix (same sign-in constraint) — both folded into the founder feel-test.
- **Flags:** schema change? **no.** security-relevant? **no** (analytics + test script + a client-side recursion fix). MCP surface changed? **no.**
- **New parking-lot entries:** none (two existing entries updated: stale assertion ✅, optional events partially ✅).
- **Recommended next session:** founder feel-test (now including "+"-insert + an undo of a dismiss to see the curation event) → then P1.3 viral loop v0.

### 2026-07-13 — P0.2 close-out: G1 dashboards + Design partners cohort built in PostHog
- **Items touched:** P0.2 🔵→✅ — the last open piece ("founder builds the §6 dashboards + cohort") was done directly in the founder's PostHog project (EU, id 137017) via the PostHog MCP, with the founder in session.
- **What was built (per `docs/G1-MEASUREMENT.md` §3/§5/§6):**
  - **Cohort "Design partners" (id 180915)** — dynamic + self-maintaining: person `is_partner=true` AND email NOT IN {greg@getpinpoint.com, greg.manzanera@gmail.com, reviewslidehuddle@mailinator.com} (the mailinator address is the "JC test account" — confirmed as the only other identified person in PostHog). Adding a real partner to `FEED_PARTNER_EMAILS` auto-enrols them once they sign in; no PostHog edit needed.
  - **Dashboard "G1 — Phase 1 validation" (id 816687, pinned)** with 4 insights, all cohort-filtered: (1) Completed rounds/week — `version_published` weekly bar (north star); (2) In-product feedback by round & surface — `feedback_added` broken down `version`×`surface`; (3) Core-loop funnel — landed→feedback→send-to-AI→published, **aggregated per deck** (`funnelAggregateByHogQL: properties.deck_id`, the spec's "per huddle"), 30-day window; (4) Feed-landing health — engage rate (B/A) vs bail rate (C/A) formulas over feed landings.
  - The optional §6.5 curation insight was NOT built (`feedback_curated` event doesn't exist yet — already in the Parking Lot 2026-06-23).
- **Files changed:** none in the repo (all artifacts live in PostHog); this tracker.
- **Verified by:** all 5 G1 events confirmed present in the live project schema; every insight query executed green before saving; the `version`×`surface` breakdown returned real founder-test data (e.g. 5×"7·deck" w/c 2026-07-05); the unfiltered funnel converted 1 full loop end-to-end (3 landed → 1 published, median 5m58s), proving the funnel machinery + event chain work. Cohort-filtered tiles correctly read zero (cohort is empty until a real partner exists).
- **Flags:** schema change? **no.** security-relevant? **no** (analytics-config only; no code touched). MCP surface changed? **no.**
- **New parking-lot entries:** one (below) — when the first real partner is added to `FEED_PARTNER_EMAILS`, the founder's own emails should be REMOVED from that allowlist, else founder events carry `is_partner=true` and pollute the event-property side of the measurement (the cohort excludes him, but funnel/step-level `is_partner` filters would not).
- **Recommended next session:** the outstanding founder feel-test of the 2026-07-05 viewer batch + the P0.5 PDF fidelity check; then P1.3 (viral loop v0).

### 2026-07-05 — Undo (Ctrl+Z / ⌘Z) for viewer actions (founder-requested)
- **Items touched:** none on the master plan — founder asked for OS-native undo of viewer actions (reversing a delete, removing the last comment). Assessed + planned first (scope table, caveats), built on go-ahead.
- **What landed (BEHAVIOURS B15):** a session-local undo stack (depth 20) in the floating viewer. Every action YOU take pushes its inverse; Ctrl+Z / ⌘Z pops it: add↔delete for comments/flags/stubs (both directions), owner dismiss/restore flips, owner edit-sent-text restores the previous value. Inverses reuse the SAME RLS-checked write paths (undo can never exceed your permissions). **Guards:** inactive while typing (native text-undo owns the shortcut in inputs); Shift/Alt chords ignored (no redo v1); re-creates pass a new `{track:false}` option threaded through all three write hooks so undo cycles never inflate Gate-G1 `feedback_added`. A small "↶ {what happened}" toast (bottom-centre, ~2.2s) names the change, since it may be on another slide. Honest limits documented: your actions only, this tab only, restored items are re-creations (new id/timestamp). Hook changes: `addComment`/`addFlag` now return the saved id (undo needs it); all three creators accept `opts.track`.
- **Files changed:** `FloatingViewer.tsx` (stack + wrapped `*U` handlers at every mutation call site + keydown + toast), `useDeckComments.ts`, `useDeckFlags.ts`, `useDeckStubs.ts`, `docs/BEHAVIOURS.md` (B15 new), this tracker.
- **Verified by:** tsc + eslint clean. Live: posted a comment → Ctrl+Z → comment gone + toast "↶ Comment removed"; posted again, deleted it via its Delete link → Ctrl+Z → comment restored + toast "↶ Comment restored"; test data then cleaned for real. Typing guard by construction (the keydown skips inputs/textareas).
- **Flags:** schema change? **no.** security-relevant? **no** — inverses are the existing RLS-checked paths only. MCP surface changed? **no.**
- **New parking-lot entries:** none (redo + cross-reload undo noted in B15 as v1 limits, not parked — add if partners ask).
- **Recommended next session:** founder feel-test, then commit the day's batch.

### 2026-07-05 — Deck→feed slide sync + flag Delete link (founder polish)
- **Items touched:** none on the master plan — two founder-reported issues on the spectrum. (1) navigating slides on the stage left the feed stranded (sync only ran feed→deck); (2) no simple way to delete a removal flag.
- **Deck→feed sync (BEHAVIOURS J4b):** `FeedStream` gains `activeSlideIndex`/`activeVersion` props; on change it scrolls the on-stage slide's cluster to the top (comments beneath). Guards: **feed-originated selections are marked (`markSelfNav`) so a card click never scrolls the feed back**; first effect run is skipped (mount's open-at-current-round wins); a slide with no cluster in that round is a no-op. **Debug note:** first attempt used `behavior:"smooth"` + `requestAnimationFrame` — both failed live (the feed's frequent re-renders cancel a smooth animation AND perpetually cancel the rAF), so it's a **direct, instant `scrollTo`** (also better UX — snaps with rapid flips). Verified live: slide 4 (near feed bottom) clamps to max; slide 1 moves the feed to its cluster (3017); card clicks don't fight it.
- **Flag Delete (BEHAVIOURS C5):** the person who RAISED a flag now gets a one-click **"Delete"** on the flag card in the floating comments panel (mirrors "Delete" on your own comment; wired to the existing `removeFlag`, RLS-flagger-only). **Scope flag:** an owner deleting a *different* person's flag is NOT offered — `removeFlag` is RLS-restricted to the flagger, so it would silently fail; the owner's tool for others' flags stays Dismiss. A true owner-delete would need a server-action/RLS change (security-sensitive) — noted, not done. Verified live: raised a flag as the owner→flagger, Delete link appeared (rust), clicking it removed the flag (test data self-cleaned).
- **Files changed:** `FeedStream.tsx`, `FloatingViewer.tsx`, `CommentsPanel.tsx` (`onFlagDelete`), `docs/BEHAVIOURS.md` (J4b new, C5), this tracker.
- **Flags:** schema change? **no.** security-relevant? **noted, not changed** — flag delete uses the existing RLS-flagger-only path; owner-delete deliberately NOT added (would need an RLS/server-action change). MCP surface changed? **no.**
- **New parking-lot entries:** *Owner delete of a collaborator's removal flag* — needs a `setFlagCurationAction`-style server action (or RLS policy) so the owner can hard-delete, not just Dismiss. P2/P3 (curation).
- **Recommended next session:** founder feel-test both, then commit the day's uncommitted batch.

### 2026-07-05 — Join toast: "{Name} is viewing the deck" (founder-requested, rides the presence system)
- **Items touched:** none on the master plan — founder asked for "notify logged-in users when another user accesses the document"; assessed (makes sense: presence's proactive half; scoped to an IN-SESSION toast — email/push for absent users is P5.1 + needs P2.2), planned with guardrails, built on go-ahead.
- **What landed:** new `PresenceNudge.tsx` (visual clone of the comment nudge: one coalescing toast, bottom-right, ~5s dissolve — shorter, nothing to act on — hover-pause, reduced-motion aware, ✕ only, no action button); join detection in `FloatingViewer` diffs `useDeckPresence`'s live set. **Guardrails:** zero toasts on your own arrival (first-sync + 5s settle window swallows the existing roster); 5-min per-person debounce (refresh/version-hop rejoins stay silent); never yourself; anonymous viewers never trigger (they're never announced); **comments win the shared toast slot** (joins dropped while a comment nudge shows; an arriving comment clears a showing join toast — the two can never stack). Analytics: `presence_nudge_shown`. BEHAVIOURS **B14** new.
- **Files changed:** NEW `web/src/app/viewer/PresenceNudge.tsx`; MODIFIED `FloatingViewer.tsx` (join-diff effect + slot rules), `docs/BEHAVIOURS.md` (B14), this tracker.
- **Verified by:** tsc + eslint clean. Live end-to-end: preview signed in as the owner, reloaded, waited past the settle window, then a client joined the presence room as the offline collaborator → toast "Jpcastrog is viewing the deck" appeared (~6s incl. sync) with avatar + ✕, auto-dissolved ≤6s later; no spurious toasts for the already-online roster at load.
- **Flags:** schema change? **no.** security-relevant? **no** (reads the existing presence room; nothing stored). MCP surface changed? **no.**
- **New parking-lot entries:** none.
- **Recommended next session:** founder two-browser feel-test, then commit today's uncommitted batch ("+" icon evolution → floating comments redesign + refinements → presence-dot consolidation → join toast).

### 2026-07-05 — Floating comments redesign: headerless floating cards + one-click, keyboard-first composer (founder mockup round)
- **Items touched:** none on the master plan — a founder-requested refresh of the floating viewer's comments surface (assessed + mocked first; founder clarified mid-plan that the slide "+" keeps its dropdown — the change is what happens AFTER "Add a comment"). Also same-day, earlier: the slide "+" button itself went through a mockup round → comment-bubble-with-plus icon, 44px, always visible, deep shadow (B13 updated at each step).
- **What landed (floating skin only; classic panel untouched):** (1) **No container box, no header** — each comment is its own white card with a soft shadow floating directly over the slide (Google-Slides-like); the flag keeps its rust card in the flow; the old "Slide N · count · flagged" header is replaced by a small ghost ✕ (orientation comes from the slide / the feed's cluster scroll). Read-only/orphan/signed-out footers became floating cards too. (2) **One-click composing:** "+ menu → Add a comment" opens the composer ALREADY FOCUSED (`composeNonce` prop — bumped only by that path; the arrival banner / comment nudge open without stealing focus). (3) **Keyboard-first:** Enter sends · Shift+Enter new line · Esc discards the draft and closes the composer; Esc with an empty composer (or at panel level, guarded against open popovers/typing) closes the surface; a "Send" button + a hint line remain for mouse users. New comments append at the bottom; the column scrolls; post → scroll-to-newest kept.
- **Files changed:** `CommentsPanel.tsx` (header branch, card shadows, composer, `composeNonce`), `FloatingViewer.tsx` (transparent wrapper, `composeNonce` bump, global Escape), `docs/BEHAVIOURS.md` (C1 + C2 rewritten, B13 tweak), this tracker.
- **Verified by:** tsc + eslint clean. Browser end-to-end signed in: + → Add a comment → panel opens with NO box chrome/header, ghost ✕, composer focused (activeElement check), 7 floating shadow cards; typed + Enter → comment posted, appended at bottom — then deleted via its own Delete (test data cleaned); Esc with a draft → composer closed + draft discarded, panel stayed; second Esc → panel closed. Screenshot matches the approved mockup.
- **Refinement round (same day, founder ×3):** (1) after sending, the composer **stays open, empties, and refocuses** — ready for the next comment (was collapse-on-send; verified: posted via Enter, composer still open + empty + activeElement); (2) new line = **Alt+Enter** (was Shift+Enter; inserted manually via `setRangeText` — browsers don't insert on Alt+Enter natively; verified "line one\n"); (3) the keyboard-hint copy line removed (verified absent). BEHAVIOURS C2 updated again.
- **Refinement round 2 (same day, founder ×2):** (1) the composer's focus outline was getting overlapped by scrolled comment cards (founder screenshot) — the outside `ring` became an **inside 2px border + `z-10`**, so scrolled cards can never cover it (verified: border 2px purple, z-index 10, top visible with the list scrolled to bottom); (2) **presence dots stripped from ALL content surfaces** — the always-on "signed in" dot on your own feed/panel avatars read as presence and contradicted the rail (the rail's GR dot was in fact CORRECT — the other account genuinely had the deck open); now the green dot renders only where `online` is passed = the rail (YOU always dotted there; you're here by definition). Verified: rail dots correct incl. "online now" tooltip; **0 green dots across 39 feed avatars and the whole panel**. BEHAVIOURS G3/G4/G9/A5 updated.
- **Flags:** schema change? **no.** security-relevant? **no** (display + focus/keyboard only; same write paths). MCP surface changed? **no.**
- **New parking-lot entries:** none.
- **Recommended next session:** founder feel-test of the whole 2026-07-05 batch, then commit (this + the "+"-button icon evolution are uncommitted; the A/B/C/presence commits are already pushed).

### 2026-07-05 — Presence system: the "online now" green dot (parking-lot item, founder-promoted)
- **Items touched:** none on the master plan — the 2026-07-03 parking-lot presence item, promoted by the founder after a plan round. Founder decisions at plan time: **dot only** (no offline dimming — dimming keeps its Slice-A meaning, "silent") and **roster surfaces only** (rail + Huddlers cluster; never feed cards / panel — historical records don't wear live signals).
- **What landed:** new **`useDeckPresence`** hook — everyone signed-in viewing a deck joins the Supabase Realtime **Presence** room `presence:deck:{id}` (user id + freshness timestamp, re-announced every 30s; same channel hygiene as `useDeckComments` incl. token-refresh); `Avatar` gained an `online` prop (the same green dot as `self`); rail + cluster pass it through with "· online now" tooltips; wired in `FloatingViewer` + `DeckFeed`. **Freshness TTL (found in verification):** clean leaves clear in seconds, but an abruptly-killed client left a **server-side phantom that never reaped during a 2-minute watch** — so readers only count entries stamped within 75s (stamp-less = phantom), bounding a crashed viewer's dot at ≤ ~90s. Privacy per the parked note: user id only (never an email), anonymous viewers never announced; caveat documented (G9): the room isn't RLS-gated — protected by the unguessable deck id, payload a bare user id; Realtime Authorization is a possible future security-relevant upgrade. Nothing stored — no tables, no schema.
- **Files changed:** NEW `web/src/app/viewer/useDeckPresence.ts`; MODIFIED `Avatar.tsx` (`online` prop), `HuddleFilterStack.tsx`, `HuddleAvatars.tsx`, `FloatingViewer.tsx`, `DeckFeed.tsx`, `docs/BEHAVIOURS.md` (**G9 new**; G4 + J8 "reserved" notes flipped to live; G3 comment), `docs/design-system.md` (§2.5 rule 3 "live" note), this tracker (+ parking-lot row marked done).
- **Verified by:** tsc + eslint clean. Live end-to-end in the browser preview (signed in as the owner) against a real second account simulated by a Node client joining the same room: dot appeared on their rail avatar with tooltip "Greg · online now · …"; offline member, the AI, and the invite seat stayed dot-free; navigating the browser away removed its own entry from the room within seconds (observer script); an abrupt kill cleared the dot promptly (and is TTL-bounded regardless — the phantom-entry failure mode was reproduced first, then fixed and re-tested with stamped announcements).
- **Flags:** schema change? **no.** security-relevant? **one caveat, documented not changed** — a new non-RLS-gated Realtime presence room (unguessable-deck-id capability model, user-id-only payload; see G9); no auth/RLS/service-role/MCP changes. MCP surface changed? **no.**
- **New parking-lot entries:** none (the presence row is marked done; the anonymous "N reviewing" live count remains a possible later extension, noted in G9's scope).
- **Recommended next session:** founder two-browser test (steps in the session summary), then commit everything (colour-discipline slices A–C + presence sit together uncommitted).

### 2026-07-05 — Colour discipline Slice A: §2.5 rules + the huddler rail rebuild + achromatic history
- **Items touched:** none on the master plan — the founder's colour-discipline/chrome-by-scope brief, staged A/B/C; **Slice A built** (rules + rail + achromatic history), Slices B (slide-anchored clusters) + C (chrome by scope) pending founder test + go-ahead. Two founder decisions taken at plan time: **per-huddle join-order colour assignment** (over global per-person) and **settled items stay grey on hover** (the one-way rule; colour-on-hover restore retired). One founder override vs the brief: **the AI keeps its rail seat** (logo/AI mark + its click-to-filter — human/AI collaboration stays visible); its badge goes neutral grey like everyone's.
- **What landed:** (1) **`design-system.md` §2.5 "Colour discipline"** — 9 governing rules (actors-not-states, achromatic history, presence exception, amber=AI incl. its rail seat, one filled purple, pills-not-metadata, colour-never-only-channel, colour budget, position answers scope) + the OKLCH palette spec; marked as overriding older sections. (2) **Avatar rebuilt**: owner = the one FILLED purple circle (white initials, star removed); collaborators = OUTLINE circles in an 8-colour OKLCH max-distance sequence (blue→crimson→green→magenta→azure→olive→rust→slate; no purple/amber families; rings ≥3:1, initials ≥4.5:1), assigned **per-huddle by join order** (new `person-colors.tsx`: context provider per surface + deterministic hash fallback; `getDeckParticipants` now resolves `joinedAt` = earliest share/comment so every viewer/surface agrees). The 2026-07-03 pastel+star language is retired. CommentsPanel's classic-path local letter-avatar migrated to the shared Avatar (classic `SlideViewer.tsx` itself untouched). (3) **Rail**: neutral grey satellite badges for everyone (AI's version count included — was teal/purple); ordering = owner pinned, then contribution desc, **frozen on load** (no mid-session hopping; badges stay live); zero-contribution members sink, dim to 55%, chip-less; tooltips gain the per-kind breakdown ("2 comments · 1 request") via per-kind `contributionCounts`; selection halo now purple (was teal). (4) **Achromatic history**: settled feed items/version events never regain hue (hover lifts dim only); "Addressed in vN" is quiet grey with the "→" on hover/keyboard focus; type chips + stub/flag thumbnails grey out when settled. Muting *logic* unchanged (A4/A6 conditions identical).
- **Files changed:** NEW `web/src/app/viewer/person-colors.tsx`; MODIFIED `Avatar.tsx`, `HuddleFilterStack.tsx`, `FloatingViewer.tsx`, `DeckFeed.tsx`, `CommentsPanel.tsx`, `FeedItemCard.tsx`, `VersionSpineEvent.tsx`, `web/src/lib/slide-store.ts` (participants `joinedAt`), `docs/design-system.md` (§2.5), `docs/BEHAVIOURS.md` (A4, A6, G3, G4, J8), this tracker.
- **Verified by:** tsc + eslint clean. Browser preview at 1400×900 on a real 2-collaborator deck (signed in via a locally-generated Supabase session): owner filled `#4A3FB5` everywhere/no star; collaborators outline blue `#2563EB` (joiner 1) + crimson `#BE123C` (joiner 2) — identical colours in the spectrum stack, feed cards, and the standalone `?view=feed`; rail order YOU→AI→contributors desc with the silent member at 0.55/chip-less; all badges `#E7E7EA` grey incl. the AI's; tooltips show the breakdown; filter click → purple halo + 0.35 dim + chip, clears clean; "Addressed in vN" grey `#6B6B75` with arrow opacity 0 at rest; settled chips grey; zero `hover:[filter:none]` left in the DOM. Console clean except the sandbox's known Realtime transport noise (pre-existing).
- **Flags:** schema change? **no** (reads two existing `created_at` columns). security-relevant? **no** — display-only; `getDeckParticipants` change adds a timestamp to a payload already gated to signed-in huddle members; no auth/RLS/service-role change. MCP surface changed? **no.**
- **New parking-lot entries:** none.
- **Recommended next session:** founder end-to-end test of all three slices with 2–3 accounts (combined steps in the Slice C session summary), then commit. Note: the brief's four reference mockup HTML files aren't in the repo — built from the written spec. *(Slices B + C were subsequently green-lit and built the same session — see the blocks below.)*
- **Slice C (same day, founder go-ahead) — chrome by scope:** (1) **Top-right = identity + deck-scope only**: the Export icon moved from the top-left title cluster into the top-right collapsible (deck-scope corner; label still version-aware), order now huddle · export · Send to AI · **Share** (the one filled purple) · **account avatar** (moved out of the collapsible — identity persists, never fades). (2) **The avatar menu absorbed "Pin toolbars"** (new optional `viewerSettings` prop on the shared `AvatarMenu`; viewer-only) and the **stray bottom settings gear was removed** (it held only that toggle, so no separate "Settings" item was added — nothing else to hold; noted caveat: anonymous viewers lose the pin control since they have no avatar menu). (3) **New slide-scope "+" (`SlideActionsMenu.tsx`)**: a purple "+" bottom-right of the slide (where the old "…" sat, same hover-reveal) opens a menu labelled "THIS SLIDE" — Add a comment (opens the existing panel/composer) · Flag for removal (the shared `FlagPanel`, extracted from `SlideFlagControl` so classic + floating share one copy; classic's "…" unchanged) · Request a slide after this (the shared `InsertStubForm`, inserts at slide+1, focuses the stub — same path as the rail's "+"). **No new write paths.** (4) **The unscoped comments button ("💬 N") retired, not relocated** — panel still opens via the "+" menu / arrival banner / comment nudge, and closes via its own ✕, so nothing orphaned. BEHAVIOURS: B5 + B7 rewritten, **B13 new**, C6 (classic-only + FlagPanel), E3 (placement), G2 (menu), B12 (stale gear refs); design-system §3.2 revised to match §2.5. Verified in preview signed-in on the 2-collaborator deck (deck view + spectrum): export link in top-right ✓, comments button + gear absent ✓, Share filled `#4A3FB5` ✓, purple "+" `#4A3FB5` on the slide ✓, menu shows THIS SLIDE + 3 items ✓, flag view renders the shared reason form ✓, request view renders the stub form ✓, Add a comment opens the panel (existing comments + composer + Close ✓); spectrum: stack/feed unaffected, console clean; tsc + lint clean.
- **Slice B (same day, founder go-ahead):** the feed's flat per-item cards became **slide-anchored clusters** (BEHAVIOURS A5 rewritten). Per round: one cluster per slide — header = the slide's thumbnail declared once (168px anchor, click = peek), **"Slide N · {title}" plain text** (title = the slide's first heading, extracted client-side, best-effort) + a change count ("2 comments · 1 removal") — items fall out beneath on a quiet connector line as **rows**: bare type icon in the gutter (kind colour live / grey settled; type chips + slide pills gone from the feed), avatar + name + time + text as before. **Requested slides are their own cluster at their position**, the dashed-teal preview as their header identity ("Requested slide · after slide N" plain text). Implementation: `FeedStream.tsx` groups the round's ALREADY slide-sorted items (`buildSlideClusters` — consecutive runs; ordering logic untouched, A2 unchanged) + renders headers; `FeedItemCard.tsx` gained `layout="row"` (selection ring, muting, curation, resolution line all identical; the boxed card layout remains but is unused). Both feed surfaces get it via the shared `FeedStream`. Verified in preview on the 2-collaborator deck: clusters per slide with one thumbnail each + real titles ("Slide 1 · Competitive Benchmark"), stubs clustered at "after slide 5"/"after slide 8", 0 chips/pills in the DOM, gutter icons teal (live) / grey (settled), row click rings + drives the stage (older round → v2&slide=0 navigation observed), owner curation buttons present on current-round rows, 17 muted elements keep grayscale; tsc + lint clean.

### 2026-07-04 — In-session comment nudge (parking-lot item, founder-promoted): live "you got a comment" toast
- **Items touched:** none on the master plan — the 2026-07-02 parking-lot item "In-session 'new comment' notification", promoted by the founder and built. Design round first (mockups: placement A top-centre vs B bottom-right; founder chose **B**, confirmed the coalescing answer to "do they stack?").
- **What landed:** NEW `CommentNudge.tsx` — ONE bottom-right toast when a teammate's comment lands live: shared Avatar + "{Name} commented on slide {N}" + 64-char snippet + teal **View** + ✕; dissolves ~7s (hover pauses); rapid arrivals **coalesce** into "N new comments from A and B" (never a stack); View jumps the deck to the newest comment's slide + opens the panel there. Suppressed: own comments, spectrum feed mode (card pops into the visible live feed), panel-open-on-that-slide, read-only/anon. Event source: new optional `onRemoteInsert` on `useDeckComments` (fires for other-people INSERTs on the viewed version; ref-held so the channel never resubscribes). Suppression flags read through `nudgeCtxRef` (kept fresh by an effect) — a first attempt with forward-referenced render consts broke React Compiler memoization of unrelated divider callbacks (lint caught it); the ref pattern fixed it. Also fixed the hook's missing `currentUserId` channel dep. Analytics: `comment_nudge_shown`/`comment_nudge_clicked`.
- **Files changed:** NEW `web/src/app/viewer/CommentNudge.tsx`; MODIFIED `web/src/app/viewer/FloatingViewer.tsx` (state/wiring/render), `web/src/app/viewer/useDeckComments.ts` (`onRemoteInsert` + dep fix); `docs/BEHAVIOURS.md` (new B12), this tracker. **Uncommitted.**
- **Verified by:** tsc + eslint clean. Signed-in browser e2e (temp collaborator via session cookie on the safe smoke-test deck; comments inserted externally as the owner via service role): toast appeared LIVE bottom-right (16px/56px from edges) with correct name/slide/snippet; two rapid inserts → **one** toast "2 new comments from Greg and Rita" (max simultaneous toasts observed: 1); View → slide counter 3/3 + panel showing the new comment + toast gone; spectrum feed mode → card appeared live in the feed with the toast suppressed throughout. Anon regression: viewer loads, no nudge, signed-out chrome. Artifacts fully cleaned (7 test comments, 1 share row, temp user deleted; cookie cleared). Console CHANNEL_ERRORs observed were the deleted-test-user's poisoned socket (same known artifact as the 2026-07-03 session), not a user-facing path.
- **Flags:** schema change? **no.** security-relevant? **no new surface** — presentation over the existing RLS-gated realtime stream; own-comment filtering client-side; anonymous viewers receive nothing new. The e2e used the local service-role key on the safe smoke-test deck with full cleanup (established pattern). MCP surface changed? **no.** Classic untouched.
- **New parking-lot entries:** none.
- **Recommended next session:** commit the nudge; founder eyeball on a real two-person session; then P1.3 viral loop v0 (next unstarted P1 item).

### 2026-07-03 — PDF export (P0.5 revived): Export button + print view, completing the loop
- **Items touched:** P0.5 ⬜ deferred → 🔵 — founder revived it 2026-07-03 (completes capture → review → curate → send → revise → **export**). Approach chosen after a plan round: **print view + the browser's own "Save as PDF"** (Option 2) — a serverless headless-browser renderer was weighed and declined for now (heavy dependency, cold starts, timeout risk, and an SSRF surface from running untrusted slide HTML server-side); it remains the natural upgrade and would drive this same print route. Also this session (separate, committed earlier): the generic AI mark restyle (lilac square + purple "AI", commit c7ff45e).
- **What landed:** (1) **Export icon** in the floating viewer's top-left collapsible, right of the version chip — appears/disappears with the title (founder placement call); tooltip "Export v{N} as PDF" (version-aware); opens the print view in a new tab; hidden on empty decks; fires `export_pdf_clicked`. (2) **`/viewer/print?id&v`** — renders every slide of that version at natural canvas size, one per page, via the same `parseDeck`+`buildSrcdoc`+sandboxed-iframe path; auto-opens the print dialog after all slides load (+1.5s settle); PDF filename "{title} — v{N}" (re-asserted at print time — Next's metadata overwrites a mount-time title); sticky screen-only top bar with loading/ready/error states; `&autoprint=0` internal testing hatch. Classic viewer untouched.
- **Files changed:** NEW `web/src/app/viewer/print/page.tsx`, `web/src/app/viewer/print/PrintView.tsx`; MODIFIED `web/src/app/viewer/FloatingViewer.tsx` (ExportIcon + link), `docs/BEHAVIOURS.md` (new E3), this tracker.
- **Verified by:** tsc + eslint clean. Browser preview on the safe smoke deck (3 versions): print page renders 3 sheets at 1280×720 with breaks page/page/auto (no trailing blank); `?v=1` → 2 sheets + bar "· v1" + title "[G1 smoke test — safe to delete] — v1" at print time (print stubbed) — version lens proven; bad-id → "Couldn't export / This deck couldn't be found."; viewer button href/label follow the viewed version (v3 → v1 checked); auto-print observed opening the real dialog; zero console errors. NOT yet verified: a human saving an actual PDF and eyeballing fidelity → founder test steps in the session summary; P0.5 stays 🔵.
- **Flags:** schema change? **no.** security-relevant? **reviewed, no new surface** — the print page reads decks exactly like `/viewer` (same by-id fetches + `?v=` lens; export = a read of an already-viewable deck); iframe sandbox unchanged (`allow-scripts`, never `allow-same-origin`); no auth/RLS/service-role/MCP change. A read-only Supabase REST query (service key from `.env.local`) was used to list deck ids for testing — no writes. MCP surface changed? **no.**
- **Fix round (same day, founder-reported):** founder's first real export produced **blank/cropped pages**. Diagnosis from his print-dialog screenshot: destination was **"Microsoft Print to PDF"** — a printer driver that forces A4/Letter portrait and ignores the slide-sized `@page`, so slides were rescaled/cropped onto the wrong paper (the browser's built-in **"Save as PDF"** destination honours the custom page size). Two fixes: (1) the ready-state copy now instructs the destination explicitly — "ready — in the print dialog set Destination to “Save as PDF” (not a printer), then Save."; (2) before printing, the page now **walks every sheet through the viewport** (~120ms each) then returns to top — Chromium rasterizes offscreen sandboxed iframes lazily, which can print far-down slides as blank pages even with the right destination. Verified in preview (print stubbed): walk runs, returns to scrollY 0, title correct, print fires; tsc+lint clean. BEHAVIOURS E3 updated (behaviour + copy).
- **Fix round 2 (same day, founder-reported):** the retry printed complete pages but **rotated 90° on portrait paper** — printer-driver destinations normalise landscape custom-@page content onto portrait A4/Letter and rotate it. Root decision: stop depending on dialog settings entirely — page geometry moved from slide-sized custom `@page` boxes to **A4 landscape** (a named size + orientation keyword every destination understands), each slide scaled-to-fit and centred with thin white letterbox bars (A4 1.41:1 vs 16:9; ~21mm top/bottom). The on-screen sheets now show the same A4+letterbox shape (honest preview). Copy relaxed to "choose “Save as PDF” (or any PDF destination)". Verified in preview: `@page {size: A4 landscape}`, sheets 296×209mm (1119×790px), slide 1119×629px centred (scale 0.874), 80px symmetric bars, sandbox unchanged; tsc+lint clean. Trade-off flagged to founder: full-bleed 16:9 pages exactly matching the slide aspect need the server-side renderer upgrade (declined for now, see plan).
- **Fix round 3 (same day, founder-reported ×2):** (a) PDFs still opened sideways — the founder was still on **"Microsoft Print to PDF"** (Windows' default destination), whose driver rotates landscape output regardless of `@page` (a known driver bug; no CSS reach). (b) After saving, **the print dialog re-appeared** — the auto-print and the manual button could each queue a `window.print()`. Root-cause read: the auto-opening dialog itself was the design flaw — it surprises the user before any guidance is readable, with the wrong destination preselected by the OS. **Flow changed to click-through:** no auto-print (the `&autoprint=0` hatch removed with it); a **ready banner** above the sheets (slim single row, sheet-width — founder sizing call) carries the one critical instruction ("set Destination → “Save as PDF”. Printer destinations (like “Microsoft Print to PDF”) turn the slides sideways.") and the dialog opens only on click; while the pre-print warm-up walk runs, buttons read "Preparing…" and re-clicks are ignored (kills the queued-second-dialog bug); on dialog close (`afterprint`) the card flips to "PDF saved? If it looks right, you can close this tab." + "Print again". Verified in preview end-to-end with a stubbed dialog: no dialog on load, card copy, click → Preparing…/second-click ignored → print fired with title "[G1 smoke test — safe to delete] — v3" → afterprint flipped the card. tsc+lint clean. BEHAVIOURS E3 rewritten (flow, states, copy). **Refinement (founder):** the banner became a slim single row (~53px, sheet-width, button right), and the destination warning is now **Windows-only** — a red ⚠ triangle + "Printing from a Windows computer? Please set Destination → “Save as PDF” — “Microsoft Print to PDF” turns the slides sideways." (the trap is a Windows-only driver; Mac/Linux see just "Ready — N slides · vN."). OS detected client-side (userAgentData.platform, falling back to platform/UA regexes; guarded against the "Darwin contains 'win'" trap — logic table-tested in preview across win/mac/linux shapes). Verified on this Windows machine: triangle #DC2626 + warning shown, 61px banner.
- **New parking-lot entries:** none.
- **Recommended next session:** founder end-to-end retry: click Export → read the card → in the dialog pick **Destination "Save as PDF"** → save; expect upright A4-landscape pages, one slide each; scrub to an older version and export *that*; then flip P0.5 → ✅. If the destination step proves too much friction for partners, promote the server-side renderer (one-click download, no dialog) — see the plan's Option 1.

### 2026-07-02 — Review follow-ups: NS1 extension fix (applied) + dedicated OAuth/MCP review → `docs/OAUTH-REVIEW-2026-07-02.md`
- **Items touched:** none on the master plan — follow-ups from the day's review (`docs/REVIEW-2026-07-02.md`). Two things landed: (1) **fixed NS1**, (2) completed the **dedicated OAuth/MCP security review** the general pass had recommended. Also: founder re-ran `docs/verify-rls.sql` → RLS confirmed live (2026-07-02).
- **NS1 fix (code changed):** `content.js` — the extension's two cross-frame capture handlers (`installIframeHandler` reply + `installCaptureReplyListener`) now check `event.origin` against a strict Claude-domains allowlist (`TRUSTED_ORIGIN_RE`: `claude.ai` + subdomains, `claudeusercontent.com`, `claudemcpcontent.com`) before acting, and the iframe now replies to the specific requesting origin instead of broadcasting slide HTML with `"*"`. Closes the cross-origin HTML-exfiltration path (June L3 → July NS1). Verified: `node --check` clean; regex unit-tested — real Claude origins + subdomains PASS, look-alikes (`evilclaude.ai`, `claude.ai.evil.com`, suffix attacks), plain `http://`, and opaque `"null"` origin all FAIL; legitimate top-frame↔artifact-frame flow passes both directions. **No user-visible behaviour changed** → no BEHAVIOURS.md entry. **Uncommitted.**
- **OAuth/MCP deep review (read-only):** NEW `docs/OAUTH-REVIEW-2026-07-02.md`. Headline: crypto core solid (fixed HMAC-SHA256, no alg-from-input, constant-time compares, type-tagged blobs), PKCE S256 enforced, redirect URIs exact-matched before redirect, every MCP tool derives identity from the verified token + enforces `loadOwnedDeck` before writes. One **medium** (M1): issuer/login-redirect/`share_url` derive from the spoofable `x-forwarded-host` with no canonical-host pinning — infra-dependent (Vercel likely mitigates); fix = pin origin to an env var. Rest are low/deliberate (no revocation, 30-day tokens, no refresh, in-memory rate limit). **No critical/high.**
- **Q2 decision (docs only):** founder chose to **accept** the classic viewer's analytics blindness rather than instrument the frozen `SlideViewer.tsx`. Documented in `docs/G1-MEASUREMENT.md` §2.4 (new caveat) + a pointer on the `feedback_added` row in §4: G1 events fire only from the floating viewer + feed; `?view=classic` feedback is unmeasured (treat classic users as *unmeasured*, not *no feedback*, when reading the gate). No code changed.
- **Files changed:** MODIFIED `content.js`, `docs/G1-MEASUREMENT.md`; NEW `docs/OAUTH-REVIEW-2026-07-02.md`; this tracker entry.
- **Verified by:** NS1 — node syntax check + regex behaviour test (extension code, not browser-preview-observable). OAuth review — read-only analysis, no execution. Q2 — documentation only.
- **Flags:** schema change? **no.** security-relevant? **yes** — one code fix (NS1, extension origin hardening) + one security report. MCP surface changed? **no** (reviewed, not modified).
- **New parking-lot entries:** none — the OAuth report's §5 recommendations (M1 origin pinning, `/login` next check, `MCP_TOKEN_SECRET` in prod, token revocation, read/write scope split) are for the founder to promote.
- **Recommended next session:** confirm Vercel strips inbound `X-Forwarded-Host` (settles M1 severity); set `MCP_TOKEN_SECRET` in prod; commit the uncommitted review/fix work.

### 2026-07-03 — Feed↔deck spectrum, Slice 3: Huddlers filter stack + soft avatar restyle + I1 fix
- **Items touched:** none on the master plan — Slice 3 of the spectrum build, to the founder-approved mock (round anatomy, contribution counts, offline-dim reserved, one far-left floating stack).
- **What landed:**
  - **Avatar restyle (founder decision — the old "shape = role" rule retired):** everyone gets a soft pastel fill + same-hue ink initials (Google/Miro-calm, no heavy rings); the **owner alone carries a thin same-colour outer ring**. One change in the shared `Avatar.tsx` → feed cards, spine events, Huddlers cluster, panel, stack all follow. AI mark unchanged. BEHAVIOURS **G4** rewritten.
  - **The Huddlers filter stack (one element = the people AND the filter):** a floating vertical pill on the far left in split/feed — total count, each avatar with a **teal contribution-count badge** (comments+stubs+flags, all versions, live, "9+"), owner first. Clicking filters the feed: selected gets a teal halo, others dim, non-matching cards dim to ~25% (hover restores; spine events never dim), sticky chip "Showing {name}'s feedback ✕" + "{N} from others dimmed". **Deck mode hides the stack entirely** (focused commenting) and the filter goes inert — never invisibly active; it returns, visible, on switching back. The top-right G4 cluster is suppressed throughout the spectrum (the huddle never shows twice). The feed region + divider shift right for the stack; drag math adjusted. NEW `HuddleFilterStack.tsx` (reuses the shared Avatar); BEHAVIOURS **J8**.
  - **I1 fixed (floating):** the floating comments panel now renders the SHARED `Avatar` (person colour + owner ring) via the `translucent` gate + a new `deckOwnerId` prop — a person reads identically in the panel, feed, and stack. Classic keeps its local letter avatar untouched (never edit classic). BEHAVIOURS **I1**.
  - **Reserved, not built:** the green "online now" dot — needs a presence system (→ Parking Lot).
- **Files changed:** NEW `web/src/app/viewer/HuddleFilterStack.tsx`; MODIFIED `Avatar.tsx` (restyle), `FloatingViewer.tsx` (filter state + counts + stack + offsets + cluster suppression), `FeedStream.tsx` (chip + filteredOut), `FeedItemCard.tsx` (dim state), `CommentsPanel.tsx` (shared avatar behind translucent); `docs/BEHAVIOURS.md` (G4, I1, new J8), this tracker. **Uncommitted** (on `spectrum-slices-1-2`).
- **Verified by:** tsc + eslint clean; **signed-in browser e2e** (temp collaborator via session cookie on the safe smoke-test deck, 3 marked test comments): stack rendered with correct order/counts ("Greg (owner) · 2", "Rt · 1"); clicking Greg → aria-pressed halo, chip, "1 from others dimmed", collaborator card greyed while Greg's stayed in colour; ✕ cleared; deck mode → stack+chip gone, rail shown; back to split → stack + filter returned still-selected; card avatars show the new soft style (pastel bg, 0px border); the floating panel showed the shared avatar with the owner ring. Anon regression: default viewer intact, no stack, 0 console errors. All test artifacts deleted (3 comments, share row, temp user; preview session cleared).
- **Flags:** schema change? **no.** security-relevant? **no new surface** — the stack renders only for signed-in viewers from the same `participants` data (anonymous viewers never receive identities); the filter is display-only. The e2e again used the local service-role key on the safe smoke-test deck with full cleanup (same pattern as the 2026-07-03 realtime session). MCP surface changed? **no.** Classic untouched (panel change gated behind `translucent`).
- **Follow-up round (same day, founder-requested):** **(1)** the filter stack moved from vertically-centred to the **top** of the left edge (top-[84px], aligned with the feed region). **(2)** A small teal **person icon** now sits beside the huddler count in the stack. **(3)** The top-right account avatar was being **clipped ("cut in white")** — diagnosed: after the G4 restyle, `AvatarMenu` (deck context) rendered the shared Avatar whose owner-ring is an outer box-shadow, and the collapsible top-right cluster clips horizontally by design. **(4)** Fixed together with the founder's ask: `AvatarMenu` is now a **distinct account chip** everywhere (purple person icon + green "signed in" dot, all inside bounds → nothing to clip; props `userId`/`ownerId` removed, call sites updated) — "your account / door to your huddles" vs "you as a huddler" are now deliberately different looks, which also RESOLVES I1's second residual divergence by design. Verified signed-in e2e (temp collaborator): stack at top=84px, person icon present, account chip icon+dot with zero overhang, filter still works, live push still delivers post-reload. Bonus: the new realtime error logging caught a poisoned websocket in the test browser (caused by my own deleted-test-user tokens, not a user-facing path) — confirmed transient; node + fresh-tab realtime healthy. All test artifacts deleted. BEHAVIOURS **G2** (rewritten), **I1.2** (resolved by design), **J8** (position + count icon).
- **Follow-up round 2 (same day, founder-requested — six items):** **(1)** YOU pinned on top of the stack, rendered as the account identity (new `Avatar self` variant: purple person icon + green dot, matching G2) — and the same face on your own feed cards + in the floating panel. **(2)** A dashed **"+" invite seat** at the stack's bottom — copies the share link with the exact Share-button toast (`CopyLinkButton variant="invite"`, one copy of the logic). **(3)** The **AI's model mark** (Claude/ChatGPT logo, latest version's provenance; exported `AiMark`/`aiName` from `VersionSpineEvent`) sits under you; clicking it filters to "Showing {AI}'s versions" (all human cards hide, spine stands alone). **(4)** Count badges now show **ACTIONABLE** contributions only (current-version live comments + open stubs/flags, dismissed/addressed excluded; tooltip "· N to action"). **(5)** The filter now **HIDES** non-matching cards instead of dimming (founder found dimming hard to navigate); chip gains "{N} from others hidden"; self-filter reads "Showing your feedback". **(6)** Current-round cards get a **light brand-purple outline** (#D8D4F2) vs grey settled history. Verified signed-in e2e (temp collaborator, 3 seeded comments): stack order You→Claude→Greg(owner)→invite; "You · 1 to action" (past-version comment correctly excluded); person-filter hid the owner's cards ("2 from others hidden"); AI filter left the spine alone ("3 feedback items hidden"); purple border rgb(216,212,242) on current cards; own card shows the person-icon avatar; invite click fires the shared copy path (clipboard write itself is denied by the headless preview — same code path as the founder-verified Share button). Artifacts cleaned (3 comments, share row, temp user, session). BEHAVIOURS **J8** (rewritten), **G4** (self rule), **A5** (purple outline). Files also touched: `Avatar.tsx` (self variant), `CopyLinkButton.tsx` (invite variant), `VersionSpineEvent.tsx` (AiMark/aiName exported + sized), `HuddleFilterStack.tsx` (rewrite), `FloatingViewer.tsx` (actionable counts + aiSource), `FeedStream.tsx` (hide + chip variants), `FeedItemCard.tsx` (currentRound border + self avatar), `CommentsPanel.tsx` (self avatar).
- **Follow-up round 3 (same day, founder-requested — five items):** **(1)** The copy-link **toast overflowed the screen** at the far-left invite anchor — `AnchoredToast` (shared) now clamps `right` so the toast always stays on-screen; verified with a stubbed clipboard: toast at left 73/right 328 of 1360, correct text — which also proves the invite→copy→toast flow end-to-end. **(2)** **Grey hairlines** in the stack: under YOU, and above the invite "+". **(3)** The **AI mark shrank to 24px** and carries a **purple version-count badge** (unique versions − 1; tooltip "{AI} · published {N} versions" — verified "Claude · published 2 versions" with badge "2" on the 3-version smoke deck). **(4)** The **owner's primary marker is now a purple star badge** bottom-left on the shared `Avatar` (ring kept as secondary); the default owner tooltip appends "· deck owner"; the stack's owner tooltip reads "{name} · deck owner — {email} · {N} to action" (verified: "Greg · deck owner — greg@getpinpoint.com · 0 to action"). **(5)** The self tooltip reads **"You — {email} · {N} to action"** (verified). tsc+eslint clean; artifacts cleaned (temp user + share row + session; no comments seeded this round). BEHAVIOURS **J8** + **G4** updated. Files: `AnchoredToast.tsx` (clamp), `Avatar.tsx` (owner star + tooltip), `HuddleFilterStack.tsx` (dividers, AI badge, tooltips), `FloatingViewer.tsx` (aiVersionCount).
- **Follow-up round 4 (same day, founder-requested — five items):** **(1)** removed the divider between the last collaborator and the invite "+". **(2)** the owner marker is now an **actual purple star** (white-outlined, no enclosing circle) and the **owner avatar ring was removed** entirely (shared `Avatar`). **(3)** the stack's **AI mark is 32px** (same as avatars) so its purple version badge lines up vertically with the collaborators' teal comment badges (verified both badges' right edge = 59px). **(4)** filtering to a **person with zero contributions** now shows a clean empty state ("No contributions from {name}…" / "You haven't contributed…" + a "Show everyone's feedback" link) instead of the empty spine (verified: self-filter with no comments → empty state, spine hidden; owner-filter with a comment → normal). **(5)** clearing a filter **scrolls the feed to the current version's round** (verified on the v7 deck: scroller 0 → 2540 on clear). Verified signed-in e2e (temp collaborator + one owner comment): stack layout You→line→Claude(32px, badge 2)→Greg(star, no ring)→invite (no line); tsc+eslint clean; `?view=feed` and `?view=spectrum` both render live with no Next build-error overlay (the transient SWC parse errors in the preview console were stale HMR-buffer replays from a mid-edit intermediate state — confirmed by checking the Next portal shadow DOM had no build-error dialog). Artifacts cleaned (1 comment, 2 share rows across both decks, temp user, session). BEHAVIOURS **G4** (star/no-ring) + **J8** (order, AI 32px, empty state, scroll-on-clear). Files: `AnchoredToast.tsx` (already, prior round), `Avatar.tsx` (star, no ring), `HuddleFilterStack.tsx` (no trailing line, one halo, AI 32px), `FeedStream.tsx` (empty-filter state + `scrollToCurrentRound` on clear).
- **New parking-lot entries:** presence system (one entry, 2026-07-03).
- **Recommended next session:** founder test of Slice 3 + follow-up rounds; commit; then the deferred items (mobile pass, text-selection comments, presence, `SPECTRUM_PARTNER_EMAILS`).

### 2026-07-03 — Spectrum: comments panel in split + live-push diagnosed, PROVEN, and hardened
- **Items touched:** none on the master plan — two founder items on the spectrum.
- **(1) Comments panel available in SPLIT:** the Slice-2 fold was over-applied (folded in split AND feed). Per founder call, the panel + Comments toggle now fold **only in feed mode**; split keeps them (the slide is a real working surface there). Verified signed-in end-to-end: split shows the button, panel opens; switch to Feed → panel folds + button hides + Next arrow returns; back to Split → the panel **returns still open**. BEHAVIOURS **J6**, **B7**.
- **(2) Live push — diagnosed empirically, mechanism PROVEN working, then hardened:** founder reported a collaborator comment still needed a refresh. Diagnosis ladder (all against the safe "[G1 smoke test]" deck, all artifacts deleted after): (a) service-role Realtime subscribe + insert → event delivered (publication fine); (b) temp collaborator user + `shared_decks` row + anon-key client + `setAuth` — the EXACT browser pattern → event delivered (RLS-gated Realtime fine); (c) **full browser repro**: preview signed in as the temp collaborator via session cookie, spectrum feed open, owner comment inserted externally → **the card appeared live, no reload — twice** (before + after the hardening). So the mechanism works in a fresh tab; the founder's failure was most plausibly a pre-fix tab OR the real robustness bug found in the hooks: **`realtime.setAuth` was called once at mount and never on token refresh → live sync silently died in any tab open past the JWT's ~1h lifetime, with no error surfaced.** Fixed in all three floating hooks (`useDeckComments/Stubs/Flags`): re-auth on `onAuthStateChange`, and channel-error logging (`CHANNEL_ERROR`/`TIMED_OUT` → console.error) so a dead channel is visible. Classic (`SlideViewer.tsx`) has the same latent staleness bug — NOT touched (never edit classic); noted for the eventual retirement. BEHAVIOURS **J7** note.
- **Files changed:** MODIFIED `web/src/app/viewer/FloatingViewer.tsx` (fold = feed-mode only), `useDeckComments.ts`, `useDeckStubs.ts`, `useDeckFlags.ts` (realtime re-auth + error logging); `docs/BEHAVIOURS.md` (J6, B7, J7 note), this tracker. **Uncommitted.**
- **Verified by:** tsc + eslint clean; the signed-in browser repro above (fold cycle + two live-push markers, zero console errors). Test artifacts fully cleaned (2 comments, 1 share row, 1 temp user deleted; preview session cleared).
- **Flags:** schema change? **no.** security-relevant? **borderline, report:** the diagnosis used the local `.env.local` service-role key to insert/delete marked test rows on the founder's explicitly-safe smoke-test deck and to create/delete a temp auth user — no policies, keys, or tables changed; RLS/redaction untouched; everything created was deleted. MCP surface changed? **no.**
- **New parking-lot entries:** none (classic's stale-token realtime noted above dies with classic).
- **Recommended next session:** founder retest with two FRESH tabs (hard-refresh both); then Slice 3.

### 2026-07-02 — Feed↔deck spectrum, Slice 2 fix round: live feed, slide-clustered rounds, card curation, chip sizing
- **Items touched:** none on the master plan — founder-test fixes on Slice 2 (4 items, same session).
- **What landed:**
  - **(1) Type chip sizing:** the "Requested slide" chip wrapped into a taller two-line pill in narrow columns (split mode). Fixed: `whitespace-nowrap shrink-0` on `FeedItemCard`'s TypeChip. Verified: both chips render 20px (one line) in split at 1360px. BEHAVIOURS **A5**.
  - **(2) Live feed:** a collaborator's comment didn't appear in the spectrum's feed (the deck side is Realtime-live; the feed column was server-seeded only). Fixed: `FloatingViewer` merges the live hook state over the seed (`spectrumComments/Stubs/Flags`) — current-version comments replace their seed subset; open stubs/flags override/append; deletions drop; resolved history kept; skipped on historical (frozen snapshot). Teammates' feedback + your own actions now appear live. BEHAVIOURS **J7**. *(A proactive in-session "new comment" toast is a separate feature → Parking Lot.)*
  - **(3) Slide-clustered rounds (the design model's middle level):** within a round the feed sorted purely by time, so a slide-4 comment appeared after an "after slide 8" request. Fixed in `feed-items.ts` `buildVersionSpine`: items now cluster by slide (comment/flag → slide index; stub → position − 0.5, i.e. the gap it points at), chronological within a slide — implementing the version → slide → time nesting from `design-decision-feed-model`. Affects the standalone feed too (aligned with the agreed model). BEHAVIOURS **A2**.
  - **(4) Owner curation on feed cards:** the owner can now Dismiss (all kinds) and Edit (comments) directly on **current-round** feed cards in the spectrum — same hover-control pattern and the SAME server actions/hooks as the comments panel (one curation path); dismissed cards gain "Won't send to AI · Restore"; cards show the owner-edited text + "· edited" (matching the panel). Past rounds + the standalone read-only feed get no controls. BEHAVIOURS **J7**, **A5**, **A6**.
- **Files changed:** MODIFIED `web/src/app/viewer/feed-items.ts` (slideAnchor ordering), `FeedItemCard.tsx` (chip nowrap, edited-text display, curation prop + hover controls + inline editor + Restore), `FeedStream.tsx` (curation plumb, current round only), `FloatingViewer.tsx` (live merge + curation pass-through); `docs/BEHAVIOURS.md` (A2, A5, A6, new J7), this tracker. **Uncommitted.**
- **Verified by:** tsc + eslint clean; anon preview at 1360px — chips one-line in split; standalone `?view=feed` unchanged (no curation buttons, no insert gaps, 6 events + peek); 0 console errors. **Founder to verify signed in:** a collaborator's comment appearing live in the feed; the slide-4-before-after-slide-8 ordering; hover Dismiss/Edit on current-round cards; Restore on a dismissed card.
- **Flags:** schema change? **no.** security-relevant? **no** — the card curation calls the SAME owner-gated server actions the panel uses (`setCommentCurationAction` etc., ownership enforced server-side); the live merge only re-arranges data already delivered under RLS/redaction rules to the same viewer. One feedback path untouched. MCP surface changed? **no.** Classic untouched.
- **New parking-lot entries:** in-session "new comment" notification (one entry, 2026-07-02).
- **Recommended next session:** founder retest of the four fixes; then Slice 3 (avatar filter + shared-Avatar fix, I1).

### 2026-07-02 — Feed↔deck spectrum, Slice 2: folding comments panel + "+" at feed fidelity + provider-neutral copy
- **Items touched:** none on the master plan — Slice 2 of the spectrum build (informal "Phase-7" converge direction). Follows Slice 1 + its 7 fixes (entry below).
- **What landed:**
  - **Folding comments panel (design model §3, "one or the other, never both"):** in the spectrum, while the left region shows the full feed (split/feed modes), the right comments panel folds away and the Comments toggle hides — the same comments are inline in the feed. The open state is remembered; collapsing back to deck mode returns the panel for the current slide. The Next arrow now hides only while the panel actually renders (also fixes it staying hidden on requested-slide cards). New BEHAVIOURS **J6**; updated **B2**, **B7**.
  - **"+" insert at feed fidelity (D3):** the current version's "published vN" spine strip in the expanded feed now has the hover "+" gaps — REUSING the rail's `InsertGap` (exported from `FloatingThumbnailStrip` with an `orientation` prop; no third copy). Same form, same insert + jump-to-stub flow; past versions and the standalone read-only feed get no gaps; hidden on historical views. Updated **D3**, **D5**.
  - **Provider-neutral copy (founder decision 2026-07-02, I3):** `CommentsPanel` + `StubSlideView` gained an `aiName` prop (default "Claude" → classic untouched); the floating viewer passes "AI", so curation reads "Won't send to AI" / "Edit what's sent to AI" etc. The feed's "Won't action" → **"Won't send to AI"** (`FeedItemCard`). Updated **A6**, **C3**, **C5**, **C7**, **I3** (marked resolved for floating+feed).
- **Files changed:** MODIFIED `web/src/app/viewer/FloatingViewer.tsx` (fold gates + aiName + feed insert), `FloatingThumbnailStrip.tsx` (InsertGap exported + column orientation), `LazyThumbnailStrip.tsx` (optional insert gaps), `VersionSpineEvent.tsx` + `FeedStream.tsx` (insert plumb-through, current version only), `CommentsPanel.tsx` + `StubSlideView.tsx` (aiName prop), `FeedItemCard.tsx` (dismissed copy); `docs/BEHAVIOURS.md` (A6, B2, B5, B7, C3, C5, C7, D3, D5, I3, J5-note→J6), this tracker. **Uncommitted** (along with all of Slice 1).
- **Verified by:** `tsc --noEmit` + eslint clean (8 files); anonymous browser preview at 1360px — feed mode shows **9 "+" gaps on the current version's strip only** (8 slides), clicking one opens the request form with the correct anonymous "Sign in to request a new slide here." prompt; deck mode rail keeps its 10 gaps (9 items); greyed "✓ Addressed in v7" intact; Next arrow present in feed mode; standalone `?view=feed` unchanged (6 events, peek, **0 gaps**); default viewer unchanged (sliver, counter, no spectrum toggle); **0 console errors**. **Not verifiable anonymously (founder to check signed in):** the fold itself (Comments button + panel need sign-in), "Won't send to AI" on dismissed items, the curation strings, actually inserting from the feed "+".
- **Flags:** schema change? **no.** security-relevant? **no** — display/copy only; the feed "+"-insert calls the SAME `insertStub` hook (`useDeckStubs`) the rail uses (same gating: `canComment`, hidden when read-only). One feedback path untouched (`selectCuratedFeedback` unchanged). MCP surface changed? **no.** Classic untouched (aiName defaults keep its strings byte-identical).
- **New parking-lot entries:** none (Slice 3 already parked).
- **Recommended next session:** founder signed-in test of the fold + copy; then Slice 3 (avatar filter + CommentsPanel shared-Avatar fix, I1).

### 2026-07-02 — Code-quality & security review (read-only) → `docs/REVIEW-2026-07-02.md`
- **Items touched:** none — review only, no code/config/behaviour changed. Full report: **[docs/REVIEW-2026-07-02.md](REVIEW-2026-07-02.md)** (builds on June's `SECURITY-REVIEW.md`).
- **Headline:** every June finding re-verifies, nothing regressed; the new feed/spectrum/hook surfaces correctly inherited the redaction + server-side-auth model; sandbox still `allow-scripts`-only everywhere. New mediums: extension iframe capture handler answers any origin (NS1), OAuth origin from forwarded headers (NS2), load errors silently swallowed by the default viewer + feed (Q1), classic-viewer feedback invisible to G1 analytics (Q2).
- **Files changed:** NEW `docs/REVIEW-2026-07-02.md`; this tracker entry. Nothing else.
- **Verified by:** n/a (read-only review; three parallel deep-read passes over web/src, extension, migrations, OAuth/MCP).
- **Flags:** schema change? **no.** security-relevant? **yes — review only, findings reported not fixed.** MCP surface changed? **no.** The **dedicated OAuth/MCP security review is still outstanding** (report §5.2 lists its seed items). Re-run of `docs/verify-rls.sql` recommended (newer migrations post-date the 2026-06-13 run).
- **New parking-lot entries:** none added by me — the report's §5 priority list is for the founder to promote.
- **Recommended next session:** founder re-runs verify-rls.sql + decides on NS1/Q1/Q2; schedule the dedicated OAuth review.

### 2026-07-02 — Feed↔deck spectrum, Slice 1: deck + feed on one resizable screen (behind `?view=spectrum`) + version-nav fixes
- **Items touched:** none on the master plan (Phases 0–6). This is the informal **"Phase-7" tidy-up direction** — converge the duplicated floating + feed renderings — **not** a formal milestone (the plan only runs 0–6). Product-track work toward the feed↔deck spectrum (`docs/design-decision-feed-model (1).md`).
- **What landed (Slice 1):** the floating viewer and the conversation feed now coexist on one resizable screen behind **`?view=spectrum`** (URL-only — nobody lands on it without the param, so the live default AND the partner feed test are unchanged). A **draggable divider** (grip + a Deck/Split/Feed toggle + ←/→ keys) shifts between three resting points (deck 16% / balanced 40% / feed 62%); the left region morphs **rail ↔ full feed** at a 210px threshold; the slide insets beside it and shrinks to a peek in feed mode. No comments-panel fold, no feed-internal changes (Slices 2–3).
- **Converge, don't copy:** the feed COLUMN was extracted out of `DeckFeed` into a shared **`FeedStream.tsx`**, now reused by both the standalone feed (`?view=feed`) and the spectrum — one copy, not two. The spectrum reuses the floating slide stage + `FloatingThumbnailStrip` as-is. No fourth rendering; classic (`SlideViewer.tsx`) untouched.
- **Follow-up fixes (same session, founder-reported):** two version-navigation bugs in the spectrum. **(1)** Selecting a version from the history dropdown dropped `&view=spectrum` → you got "transferred to the old experience". Fixed: `DeckVersionNav` gained a `viewParam` prop and appends `&view=spectrum` to every version link (verified: all 7 dropdown links now carry it; landing on `&v=3&view=spectrum` shows the older version with the red warning, still in the spectrum). **(2)** Clicking an older version's slide thumbnail in the feed didn't switch versions or update the pill — the thumbnail emitted only a slide index, losing the version. Fixed: `VersionSpineEvent` now emits `(slideIndex, version)`; `FeedStream` forwards it via a new `onSelectVersionSlide`; in the spectrum, clicking a thumbnail from a **different** version does `router.push('…&view=spectrum&v={N}&slide={i}')` (same version → just focuses the slide). Verified: clicking v1's thumbnail navigated to `&v=1&slide=0`, pill → `v1` + red warning, stayed in spectrum, opened on slide `1/4`. **(3)** Clicking a **requested-slide (stub) card** in the feed showed the neighbouring real slide instead of the requested slide — the feed mapped every card to a real slide index (`position − 1`), right for the standalone peek but wrong for the spectrum stage (which renders stubs). Fixed: `FeedStream` now emits the whole `ConvItem` (`onSelectItem`) and each host maps it — DeckFeed keeps the real-slide peek, the spectrum focuses the exact display item (a stub → its `StubSlideView` by id). Verified: clicking the "Q&A" requested card put the stage on `9 / 9 · requested slide` showing the stub card. Standalone `?view=feed` unaffected. **(4)** Clicking an older slide (or picking a version) while in split/feed **reset the layout back to deck** — the split lived in component state, lost on the version remount. Fixed: the mode is now persisted in a **`?mode=deck|split|feed`** URL param — `page.tsx` reads it → seeds `spectrumFeed.initialMode` → the viewer inits its split from it, and both the version dropdown links (`DeckVersionNav modeParam`) and the thumbnail navigation carry the current mode forward. Also tied the rail↔feed fidelity to the **mode** (deck = rail always; split/feed = feed, with the ~210px narrow-window fallback) so "Deck" no longer showed a cramped feed on wide screens. Verified: in Feed mode, clicking v1's thumbnail → `&v=1&mode=feed&slide=0`, stayed in Feed; all dropdown links carry `&mode=feed`; a fresh open (no `mode`) defaults to deck/rail. New BEHAVIOURS **J4** + updated **J1** (twice) + **J2**; MODIFIED also `web/src/app/viewer/page.tsx` (parse `mode`) — already listed. **(5)** Clicking a feed card for an **older-round / resolved requested slide** did nothing and left the pill on v7 — the focus-by-id found no open stub on the current version. Fixed: feed CARDS are now version-aware like the thumbnails — `FeedStream` passes the round's version with the item (`onSelectItem(item, itemVersion)`), and the spectrum navigates to that version (pill + stage follow, mode preserved) when it differs from the stage, else focuses in place (an open requested slide → its stub card). Verified: clicking the addressed "new slide" (After slide 5) card → `&v=6&mode=feed&slide=4`, pill → v6 + red warning, stayed in Feed; the open Q&A card still focuses its stub card on v7 (`9 / 9 · requested slide`). **(6)** The **settings gear** (pin floating bars) was hidden behind the spectrum's bottom-left feed column. Fixed: in spectrum it renders **bottom-right** (`z-40`); the normal viewer keeps it bottom-left. BEHAVIOURS **B5** updated. Files also touched: `web/src/app/viewer/FeedStream.tsx`, `DeckFeed.tsx`, `FloatingViewer.tsx` (already listed). **(7)** Viewing an older version on the stage made the **feed show that old version's thumbnails** under the current "published vN" event (and on the per-card thumbnails) — `FeedStream` parsed `rawHtml`, which in the spectrum is the STAGE's (possibly historical) version. Fixed: `FeedStream` now anchors its current-version deck to `versionsHtml[currentVersion]` (falling back to `rawHtml` only for pre-migration decks), so the feed always renders the latest version's slides regardless of what the stage shows. Verified: viewing v1 (`1 / 4`) on the stage, the "published v7 · 8 slides" event now renders **8** thumbnails (was 4). New note under BEHAVIOURS **J4**. Also reordered the balance toggle to **Feed · Split · Deck** (founder request) — BEHAVIOURS **J2** copy updated.
- **Files changed:** NEW `web/src/app/viewer/FeedStream.tsx`; MODIFIED `web/src/app/viewer/DeckFeed.tsx` (now top bar + FeedStream + peek), `web/src/app/viewer/FloatingViewer.tsx` (gated spectrum mode + version-nav callback), `web/src/app/viewer/page.tsx` (the `spectrum` data branch), `web/src/app/viewer/DeckVersionNav.tsx` (viewParam), `web/src/app/viewer/VersionSpineEvent.tsx` (emits version); `docs/BEHAVIOURS.md` (new **section J** incl. J4 version-nav + the "two viewers" intro note), this tracker. **Uncommitted.**
- **Verified by:** `tsc --noEmit` + `eslint` clean on all four files; browser preview on a real v7 deck — default `?id=` view unregressed (slides toggle, rail sliver, `v7` chip, counter `1/8` all intact); `?view=spectrum` opens in deck mode (rail with 8 thumbnails + "+" inserts), Deck→Feed morphs the rail into the full version spine (v1 "started this huddle" + "published v2…v7"), the divider value moves 16→62, the slide shrinks to a peek, the greyed "✓ Addressed in v7" item preserved; **0 console errors** (screenshot in session).
- **Flags:** schema change? **no.** security-relevant? **no** — display-only convergence; the `spectrum` branch issues the SAME RLS-gated reads as the feed branch (`getAllCommentsForDeck`, `getStubsForDeck({includeResolved})`, `getFlagsForDeck({includeResolved})`, per-version HTML) with the SAME anonymous email redaction; the extra reads happen only on the `?view=spectrum` URL. **One feedback path** untouched — Send-to-AI still runs on `selectCuratedFeedback` over the live current-version hooks (the path the MCP `get_feedback` tool shares). MCP surface changed? **no.**
- **New parking-lot entries:** Slices 2–3 + later items + the decided copy unification (one entry, 2026-07-02).
- **Recommended next session:** Slice 2 — define the deck/balanced/feed mode states and fold the right comments panel away as the feed widens (redundant with the inline feed comments), returning it in deck mode.

### 2026-06-27 — House style: `docs/HOUSE-STYLE.md` created + wired into the session-start files
- **Items touched:** none (documentation/setup only — no component, style, or logic changed). Established a single canonical "how we work" doc and pointed the session-start files at it, so the house style is actually loaded each session instead of living only in scattered notes.
- **What it is:** NEW `docs/HOUSE-STYLE.md` — non-technical-founder working method (plan-first, build incrementally, say how to test); reuse-don't-fork components + real repo filenames; security-sensitive flag-and-report (auth / Supabase RLS / service-role key / MCP surface); the **`docs/BEHAVIOURS.md` "document user-visible behaviour in the same change"** rule; and a per-task close-out (update tracker + BEHAVIOURS.md + state which BEHAVIOURS entries changed and what was security-verified).
- **Wired in:** `web/AGENTS.md` gained a prominent top pointer ("read `docs/HOUSE-STYLE.md` first…") above the managed Next.js block — this also covers `web/CLAUDE.md`, which imports AGENTS.md via `@AGENTS.md` (left unedited to avoid duplicating the message).
- **Files changed:** NEW `docs/HOUSE-STYLE.md`; MODIFIED `web/AGENTS.md`, this tracker.
- **Not done (flagged to founder):** (1) **`HANDOFF-BRIEF.md` doesn't exist in the repo** — the session-start brief is maintained externally/pasted in; no file was created. Founder to paste the one-line pointer into their copy: *"Canonical house style lives in `docs/HOUSE-STYLE.md` — read it first."* (2) **Persistent-memory pointer not added** — would guarantee the house style loads in non-`/web` sessions too; offered, awaiting founder yes. No root-level `AGENTS.md`/`CLAUDE.md` exists, so `web/AGENTS.md` is the only repo agent-instruction file.
- **Verified by:** documentation-only; no code path to run. `web/CLAUDE.md` confirmed to be `@AGENTS.md` (import), so the pointer propagates.
- **Flags:** schema? no. security? no. MCP? no.
- **New parking-lot entries:** none.
- **Recommended next session:** decide the two flagged items (create `HANDOFF-BRIEF.md` in-repo? add the memory pointer?), then resume the viewer transformation using `docs/BEHAVIOURS.md` + `docs/HOUSE-STYLE.md`.

### 2026-06-27 — Behaviour backfill: `docs/BEHAVIOURS.md` written (preservation checklist for the viewer transformation)
- **Items touched:** none (documentation-only — no component, style, copy, or logic changed). Pre-work for the upcoming viewer-UX transformation: capture every user-visible behaviour *as it stands today* so the rebuild can't silently destroy subtle texture (greyed past-version feed items + colour-on-hover, struck "✓ Addressed in vN", deck-peek helper copy, owner curation Edit/Dismiss, "Send to AI · N" split button, thumbnail purple-border/comment-badge/stub-outline/"+"-insert, avatars, version chip/history, copy-link `?source=capture` stripping, every empty/loading/error state).
- **What it is:** `docs/BEHAVIOURS.md` — a plain-language, code-grounded catalogue grouped by area (Feed · Slide view · Comments & curation · Thumbnails · Versioning · Dashboard · Shared chrome · Extension) with a consistent per-element mini-template (Element/Where/States/Behaviour/Copy/Trigger/Source). All microcopy quoted verbatim. Carries a "living document — update in the same change as any UI behaviour" header. Known oddities recorded as **Observations** (not fixed): the avatar role/shape note (now centralised in `Avatar.tsx`; residual divergences = CommentsPanel's local letter-avatar + TopNav's fallback chip); the "1 Issue" chip (a real hydration warning, already fixed 2026-06-22); "Send to Claude" vs "Send to AI" wording split; two-viewers-plus-feed duplication.
- **Files changed:** NEW `docs/BEHAVIOURS.md`; MODIFIED this tracker. (Read-only across ~35 viewer/dashboard/component/extension files to ground it; none were edited.)
- **Verified by:** documentation-only; no code path to run. Cross-checked each quoted string + state against the source file it cites; founder given 3–4 live spot-checks to confirm the doc matches reality.
- **Flags:** schema? no. security? no. MCP? no.
- **New parking-lot entries:** none (the Observations live in BEHAVIOURS.md §I, not as work items).
- **Recommended next session:** begin the viewer transformation against `docs/BEHAVIOURS.md` as the checklist — port each behaviour deliberately and update the matching entry in the same change.

### 2026-06-23 — Feed polish: model-aware version mark (Claude/ChatGPT logo + 🎉)
- **Items touched:** P1.2 (feed) — presentation only, no data/logic change. The version-spine "published" event now shows the **producing model's own logo** instead of the generic dark square: `deck_versions.source` → `/logos/claude.svg` (coral mark) or `/logos/chatgpt.svg` (OpenAI mark); unknown/pre-provenance (`null`) falls back to the generic mark via `<img>` onError (so a missing logo never breaks). Copy uses **🎉** instead of **✦**. Founder supplied the official SVGs. Per founder request the logo is **box-less and larger** (40px, transparent — no white chip/border); the generic mark also bumped to 40px for a consistent footprint.
- **Files changed:** `web/src/app/viewer/VersionSpineEvent.tsx`; NEW `web/public/logos/claude.svg`, `web/public/logos/chatgpt.svg`; docs `design-system.md` (§5 Version spine + §2.2 amber-rule exception note), `P1.2-HANDOVER.md`, this tracker.
- **Verified by:** `tsc`+`eslint` clean. Browser preview of the real feed (`c0e8f60235b045f381e8fb`): v7 (`source:claude`) renders the Claude coral logo (`img /logos/claude.svg`, computed 40×40, parent bg transparent, 0px border, loaded), copy "🎉 Claude published v7"; older `null`-source v5/v6 → generic mark + "🎉 AI published"; `/logos/chatgpt.svg` serves 200 (no chatgpt-source deck exists yet to render it live); no console errors. (Screenshots intermittently timed out on the iframe-heavy feed → DOM + computed-style verified.)
- **Flags:** schema? no. security? no. MCP? no. **Committed + pushed:** `e104205` (founder tracker entry) + `5cbae0a` (logo feature) → deploys via Vercel.
- **Recommended next session:** the two P0.2 cleanups (channel-attribution dashboard tile; exclude founder/test ids from the "Design partners" cohort), then **P1.4 — design-partner recruitment** (the real G1 unblock).

### 2026-06-23 — P0.2: measurement events repaired + verified end-to-end; G1 dashboard + cohort built (via PostHog MCP)
- **Items touched:** P0.2 (stays 🔵 — see status note) — the Gate-G1 measurement is now wired clean, firing, and verified through the full loop. Three threads: (1) CC verify-and-repair pass on the analytics events; (2) founder smoke-test firing all five events; (3) cohort + dashboard built and the core-loop funnel verified end-to-end via the PostHog MCP.
- **CC fix pass** (per `docs/cc-prompts/cc-prompt-g1-measurement-fix.md`): standardised all analytics property names to snake_case (removed the `deckId`/`commentCount`/`flagCount`/`stubCount` duplicates; `isPartner`→`is_partner`); confirmed/added the three core-loop events — `feedback_added` enriched with `surface`/`version`/`role`, `send_to_ai_clicked` added, `version_published` added **server-side in both** `update_deck` (mcp/route.ts) and the extension `?update=` path (api/slides/route.ts); `identifyUser` now sets `is_partner` as a **person** property. (Exact diff in CC's own session report.)
- **Files changed (CC):** `web/src/lib/analytics.ts` + the feedback-creation call sites + Send-to-AI handler + `web/src/app/mcp/route.ts` + `web/src/app/api/slides/route.ts` + the sign-in/identify path (confirm against CC's diff). **PostHog entities created this session (via MCP, not code):** cohort "Design partners" (id 163339, `is_partner=true`); pinned dashboard "G1 — Phase 1 validation" (id 769560) with 4 insights — Completed review rounds (gcjdJUAE), In-product feedback by surface (tT10rnMA), Core loop funnel (06VqixZE), Feed landing engage-vs-bail (yJzonOPn).
- **Verified by (the important part):** read the live PostHog taxonomy via the MCP — all three new events register; `feedback_added` carries clean `kind`/`surface`/`version`/`role`/`deck_id` with **no camelCase duplicates**; `is_partner` is a person property. Founder ran the full loop in-app; the **core-loop funnel returns 100% through all four steps for one person** (landing → feedback → send → version_published), times 27s / 31s / ~6m. This clears the key risk: **server-side `version_published` shares identity with the browser events** — if it didn't, step 4 would have dropped to zero. Verified end-to-end, not just "code written."
- **Flags:** schema change? **no** (telemetry only). security-relevant? **touched the MCP route + slides API** — but `version_published` is fire-and-forget **after** the existing ownership-gated save; no auth/RLS/service-role/MCP-permission logic changed (per the fix prompt's required checks — confirm in CC's report). MCP surface changed? tool **permissions** unchanged; a telemetry call was added inside `update_deck`.
- **New parking-lot entries:** (1) `g1_server_selftest` diagnostic event left firing — remove or gate to non-production before it pollutes data *(update: removed this session via the PostHog MCP — synthetic `g1-selftest` person + event deleted; see the verify-&-repair entry below)*; (2) founder/test accounts are `is_partner=true` (smoke-test) so they currently sit in the Design partners cohort — before real validation, ensure only genuine partners carry the flag or exclude founder/test ids; (3) channel-attribution dashboard tile to fully close P0.2 (source data already auto-captured via `$initial_utm_*`/`$virt_initial_channel_type`); (4) optional curation-usage tile — `feedback_curated` isn't wired.
- **Status note on P0.2:** measurement core (event schema + funnel + dashboards) is **done and verified end-to-end**; kept 🔵 only because the item also lists **channel attribution** (data captured, tile not built) + two cleanups remain. Promote to ✅ once those close. **Gate evidence:** G0's "events flowing" criterion is now met (gate stays founder-marked).
- **Recommended next session:** the two cleanups, then **P1.4 — design-partner recruitment** (the real G1 unblock).

### 2026-06-23 — G1 events: verify & repair pass — found + fixed a real server-side bug
- **Trigger:** live PostHog showed only old `deck_landing_viewed`/`feed_open_deck` (camelCase) and none of the 3 new events; founder asked to verify & repair.
- **Diagnosis (mostly false alarm + one real bug):** (1) The camelCase events were **stale** — all fired ≤ 09:55 UTC, ~2h BEFORE the G1 deploy (commit 12:12 UTC). Confirmed via PostHog query (event times) + grepping the **live prod bundle**, which already emits clean snake_case (`deck_landing_viewed{view,deck_id,role,version,comment_count,…}`, `feedback_added{kind,surface,deck_id,version,role}`, `is_partner`). So the client wiring was correct + deployed all along; nothing had exercised it yet. (2) **REAL BUG:** `version_published` (server-side) **never delivered**. Isolated it live: a `posthog-node` send with `flush()`-only never arrived, the same event with `shutdown()` arrived in ~20s. Cause: with `flushAt:1`, `capture()` starts the send immediately and `await flush()` resolves against an already-drained queue *before* the HTTP request completes; the serverless function then returns and the in-flight request is killed. `lib/analytics-server.ts` used flush-only → every `version_published` was silently dropped.
- **Fix:** `lib/analytics-server.ts` now uses a per-call client + **`await client.shutdown()`** (flushes AND awaits the send). No-op-without-key + never-throws kept. Commit `ba68885`, pushed/deployed. (The G1 wiring commit was `dbeb1e3`.)
- **Verified by:** ALL FIVE events confirmed live & clean in PostHog (snake_case, `is_partner` present). `version_published` (after redeploy, via MCP `update_deck`): `{deck_id, version:3, source:"claude", addressed_count:0}`, distinct_id = owner. Founder then ran the signed-in browser smoke test (partner greg@getpinpoint, `?from=feed`): **`feedback_added`** `{kind:comment, surface:feed, deck_id, version:6, role:owner, is_partner:true}`, **`send_to_ai_clicked`** `{surface:feed, deck_id, item_count:4, is_partner:true}`, **`deck_landing_viewed`** `{role:owner, version:6, is_partner:true}`. Person record confirmed: `is_partner=true` + `email=greg@getpinpoint.com` (person properties → cohort-ready). `deck_landing_viewed`/`feed_open_deck` also proven earlier via a local interceptor + live prod bundle.
- **Flags:** schema? no. security-relevant? **yes (re-touched `lib/analytics-server.ts` used by the MCP route + slides API)** — verified: no auth/RLS/service-role/permission/MCP-tool-permission change; capture still fire-and-forget AFTER the ownership-gated save; only ids/counts/enums sent (no content/emails/tokens). MCP surface? no.
- **New parking-lot entries:** (1) throwaway test deck `8ff7f040adbd4cdf8ff2de` ("[G1 smoke test — safe to delete]") — delete when convenient. (2) `feed_open_deck` in the smoke test fired with an **anonymous distinct_id** (pre-identify on the feed page) — PostHog merges it into the person on identify so cohort analysis still holds, and the spec doesn't put `is_partner` on `feed_open_deck`; but worth confirming the feed-page identify isn't racing the Open-deck click.
- **Recommended next session:** founder builds the §6 dashboards + "Design partners" cohort (is_partner / email) via the PostHog MCP. Events are proven clean → P0.2 closes once the dashboard exists.

### 2026-06-23 — Wired the Gate-G1 measurement event set (P0.2 → 🔵)
- **What:** wired the minimum G1 event set from `docs/G1-MEASUREMENT.md` §4 (saved the spec into the repo). Telemetry only — reuses the existing `lib/analytics.ts` seam; no new analytics path.
- **Identity:** on viewer landing (deck + feed), now register an **`is_partner` super-property** (rides every event) and `identifyUser(userId, { is_partner, email })`. **Email is a PERSON property only** (founder decision, to power the "Design partners" cohort) — **never** attached to individual events.
- **`feedback_added`:** the 3 deck hooks (`useDeckComments`/`Stubs`/`Flags`) now stamp **`surface` (feed\|deck)**, **`version`**, **`role`**, plus snake_case `deck_id` (kept `kind`). `surface` = feed-origin: the feed's Open-deck/Open-slide links carry **`?from=feed`**, which `page.tsx` turns into a `surface` prop passed through `FloatingViewer` → the hooks. (The feed is read-only, so "comment from the feed" = opened the deck from the feed, then commented.)
- **`send_to_ai_clicked`:** new — fires in `SendToClaudeButton.handleSend` with `{ deck_id, item_count, surface }`.
- **`version_published`:** new, **server-side** via new `lib/analytics-server.ts` (`captureServer`, posthog-node; no-op without a key, never throws, awaits flush). Fires in **both** save paths AFTER the gated save + resolution: MCP `update_deck` (attributed to `auth.userId`, `source` from the OAuth client) and the extension `?update=` route (`source:"claude"`, attributed to the deck owner via best-effort `getDeckMeta`; orphan → `deck:<id>` fallback). Props: `{ deck_id, version, source, addressed_count }`.
- **Normalised existing events to snake_case** (`deck_landing_viewed`, `feed_open_deck`): `deck_id`, `from_version`, `version`, `comment_count`/`stub_count`/`flag_count`; dropped per-event `isPartner` (now the super-property). Safe — key only went live today, no historical data/dashboard yet.
- **Files:** NEW `web/src/lib/analytics-server.ts`, `docs/G1-MEASUREMENT.md`; MODIFIED `web/src/lib/analytics.ts` (+`registerSuperProperties`), `viewer/page.tsx` (`from=feed`→`surface`), `viewer/FloatingViewer.tsx`, `viewer/DeckFeed.tsx`, `viewer/SendToClaudeButton.tsx`, `viewer/useDeckComments.ts`/`useDeckStubs.ts`/`useDeckFlags.ts`, `app/mcp/route.ts`, `app/api/slides/route.ts`, `web/package.json` (+posthog-node). Docs: this tracker.
- **Verified by:** `tsc` + `eslint` clean. Browser preview (key live locally → events also reach PostHog EU): captured the real payloads — `feed_open_deck {deck_id, from_version:6}` and `deck_landing_viewed {view:deck, deck_id, role, version:6, comment_count/stub_count/flag_count}` (correct snake_case + `version`); the feed's Open-deck link carries `&from=feed`; `POST eu.i.posthog.com/i/v0/e/ → 200` (events delivered); no console errors. Both server routes recompiled clean with the posthog-node import (`/api/slides` GET→405, `/mcp` GET→401 — method/auth, not module-load 500). **Not testable anonymously (verified by code + typecheck):** `feedback_added`{surface/version/role} + `send_to_ai_clicked` (need a signed-in session) and `version_published` (needs the authed save path) — these are the founder's signed-in §9 smoke-test.
- **Flags:** schema change? **No** — reused the existing `deck_versions.source` (added in P1.2) to populate `version_published.source`; no new columns/migrations. security-relevant? **YES (MCP route + slides API touched)** — `version_published` is fire-and-forget **after** the existing ownership gate (MCP `loadOwnedDeck`; extension `verifyDeckWriteToken`); `captureServer` is wrapped so it can never throw/block/undo a save; **no** auth/RLS/service-role/permission logic changed; events carry only ids/counts/enums (no slide content, comment text, tokens; email is a person-property via identify only, never on events). MCP surface? **Tool permissions UNCHANGED** — capture-only inside the existing `update_deck` handler; no new tools, no changed input/output schema or annotations.
- **New parking-lot entries:** classic-viewer Send-to-AI deck_id=null; optional G1 events not built; email-as-person-property revisit. (see Parking Lot)
- **Recommended next session:** founder builds the §6 PostHog dashboards + "Design partners" cohort (exclude greg/greg.manzanera/JC) and runs the §9 signed-in smoke-test (comment from the feed → `feedback_added{surface:feed}` in Live Events). Then P0.2 → ✅ and the G1 evidence engine is running.

### 2026-06-22 — Analytics live + feed wildcard + fixed two hydration errors in the viewer
- **PostHog connected (P0.2 seam → live).** Set `NEXT_PUBLIC_POSTHOG_KEY` + `NEXT_PUBLIC_POSTHOG_HOST=https://eu.i.posthog.com` (EU region) in `web/.env.local` and (founder) in Vercel. Verified end-to-end: local dev sends events to `eu.i.posthog.com` (200s observed); prod build confirmed to inline the key + EU host (grepped the live JS bundle). Analytics is no longer a silent no-op. **P0.2 still ⬜ overall** — funnel/attribution dashboards not built yet — but the pipe is open.
- **Feed wildcard.** `FEED_PARTNER_EMAILS=*` now puts EVERY signed-in viewer on the feed (anonymous link-holders still get the deck; `?view=deck` still escapes). `page.tsx` `isFeedPartner` gained a `partners.includes("*")` branch. Committed + pushed (`b1a264e`). Founder to set the Vercel var to `*` + redeploy.
- **Two REAL hydration errors found + fixed in the viewer** (the handoff's red "1 Issue" chip — it was live, not stale; the tracker hadn't recorded a fix because there wasn't one). Both are the classic SSR-vs-client mismatch ([[feedback_nextjs_ssr_browser_apis]]):
  1. **Feed** — `VersionSpineEvent` rendered relative/locale timestamps during SSR (`formatRelativeTime` → `Date.now()` + `toLocaleDateString`); server (UTC, "Jun 5") ≠ browser ("5 Jun").
  2. **Deck viewer** — `AnchoredToast` called `createPortal` on the first client render but rendered `null` on the server (pre-existing, surfaced via `CopyLinkButton`).
- **Fix:** new shared `lib/use-hydrated.ts` (`useHydrated()` via `useSyncExternalStore` — lint-clean, no setState-in-effect) → false during SSR + first client render, true after hydration. New `viewer/RelativeTime.tsx` (hydration-safe timestamp) replaces inline `formatRelativeTime` in `VersionSpineEvent`, `FeedItemCard`, `CommentsPanel` (+ `suppressHydrationWarning` on the two `<time>` elements whose `title` used locale `formatTime`). `AnchoredToast` gates its portal on `useHydrated()`.
- **Files:** NEW `web/src/lib/use-hydrated.ts`, `web/src/app/viewer/RelativeTime.tsx`; MODIFIED `web/src/app/viewer/VersionSpineEvent.tsx`, `FeedItemCard.tsx`, `CommentsPanel.tsx`, `web/src/components/AnchoredToast.tsx`, `web/src/app/viewer/page.tsx` (wildcard); docs `P1.2-HANDOVER.md` (smoke-check #5), this tracker.
- **Verified by:** `tsc` + `eslint` clean. Browser preview on a **freshly restarted** dev server (clean console): deck viewer (`/viewer?id=c0e8f60235b045f381e8fb`) AND feed (`&view=feed`) both show **no dev-tools issue badge, no error overlay, zero console errors**; feed timestamps render ("5 Jun"/"7 Jun") after hydration. (Screenshot timed out — known lazy-iframe/backgrounded-tab limit; DOM + badge + console verified instead.)
- **Flags:** schema? no. security? no. MCP? no. **Hydration fixes NOT yet committed** (awaiting founder go-ahead to push → prod deploy). Wildcard already pushed (`b1a264e`).
- **Recommended next session:** push the hydration fixes; founder sets Vercel `FEED_PARTNER_EMAILS=*` + redeploy + runs the `deck_versions.source` migration; then the prod smoke-check (handover #4 + #5 — confirm no console hydration errors on prod). After that, partners on the feed = Gate-G1 evidence.

### 2026-06-17 — P1.2: "settled history" greyscale (Item A)
- **Items touched:** P1.2 (still 🔵) — founder-approved greyscale treatment so the feed focuses attention on what's still **unaddressed** (handover Item A). Presentation-only, no data/logic change. Past-round **version messages** desaturate (`filter: grayscale(1) opacity(.65)`, driven off `isCurrent`), so only the current version keeps its amber ✦ / purple vN. **Feed item cards** take a new `muted` prop, computed in `DeckFeed` as `!round.isCurrent && (addressedIn != null || dismissed)` — so addressed/dismissed past items grey out while **unaddressed comments AND stubs AND flags stay in colour**; the current round never mutes. **Hover or selection** returns any muted element to colour (`hover:[filter:none]`; cards gate on `muted && !selected`).
- **Files:** `viewer/FeedItemCard.tsx`, `viewer/DeckFeed.tsx`, `viewer/VersionSpineEvent.tsx`; docs `design-system.md` §5, `P1.2-HANDOVER.md` (Item A → done), this tracker.
- **Verified by:** `tsc --noEmit` clean; browser preview (anon view of test deck `c0e8f60235b045f381e8fb`, `?view=feed`): computed styles confirm v1–v5 = `grayscale(1) opacity(0.65)` and current v6 = `none`; the `.hover:[filter:none]:hover { filter: none }` rule compiled (same classes the cards use); no console errors. **Not visually re-checked signed-in** (no partner login locally) — the addressed-vs-unaddressed *item-card* contrast is verified by logic + shared compiled CSS, not a screenshot. (Screenshots timed out — the lazy-iframe-heavy feed; a11y snapshot + computed styles used instead.)
- **Flags:** schema? no. security? no. MCP? no. **Not committed.**
- **New parking-lot entries:** none.
- **Recommended next session:** quick signed-in look to confirm the item-card greyscale (addressed comment greys, unaddressed stays colour); then handover items C (mobile) / E (per-round-accurate thumbnails) or P1.3 viral-loop v0.

### 2026-06-17 — P1.2: avatar polish round 2 + feed top-bar
- **Items touched:** P1.2 (still 🔵). Three founder tweaks: (1) **owner avatar pastel** — was a bright solid fill; now a soft pastel of the person's colour + ink initials (`Avatar.tsx` palette is now ink+pastel pairs; owner fills with `pastel`, initials in `ink`). (2) **collaborator background white** (was transparent) + the coloured ring. (3) **feed top-left shows the deck title only** — removed the version chip (`DeckVersionNav`) from `DeckFeed`'s top bar (the feed is cross-version; the peek/Open-deck carry the current version).
- **Files:** `viewer/Avatar.tsx`, `viewer/DeckFeed.tsx`; docs `design-system.md` §5, this tracker.
- **Verified by:** `tsc`+`eslint` clean; preview (owner view): owner avatar bg `#DBEAFE` pastel + `#2563EB` ink initials (account menu + feed match); collaborators white bg + coloured ring; top bar reads "SlideHuddle | Competitive Benchmark" with no version pill. No console errors.
- **Flags:** schema? no. security? no. MCP? no. **Not committed.**

### 2026-06-17 — P1.2: committed + pushed to main; handover written
- **Committed + pushed the full P1.2 feed work to `main`** (deploys via Vercel; CI lint+type-check runs on push). The feed is gated (`FEED_PARTNER_EMAILS`) and the schema/MCP changes are graceful (run fine before the migration), so the deploy is safe; throwaway `/feed-preview` route removed before commit; `.wip-backup/` left uncommitted as before.
- **New findable handover:** `docs/P1.2-HANDOVER.md` — full file map, founder actions (run `deck_versions.source` migration; set PostHog key; set `FEED_PARTNER_EMAILS` in Vercel), the Phase-1 completeness check, and the prioritised outstanding list for the next session.
- **Greyscale "focus on unaddressed" treatment — DEFINED for next session (founder-approved, NOT built yet):** everything in rounds *before the latest version* desaturates to grey (version messages, strips, resolved/addressed/dismissed items); **unaddressed/unresolved items stay in full colour — applies to comments AND requested slides (stubs) AND removal flags**; the latest version + its round stay colour; **hover returns a greyed item to colour**. Presentation-only (a per-item `muted` flag + grayscale CSS, cleared on hover/selected). Spec in the handover §A.
- **Files:** NEW `docs/P1.2-HANDOVER.md`; MODIFIED this tracker. (No code changes in this entry.)
- **Flags:** schema? no (this entry). security? no. MCP? no.

### 2026-06-17 — P1.2: version-spine design pass (align to the claude.ai reference)
- **Items touched:** P1.2 (still 🔵). Founder feedback vs the claude.ai reference deck: (1) version events should be flush **messages, not boxed cards**; (2) **addressed items weren't showing** (the reference strikes comments "Addressed in vN").
- **Changes:** `VersionSpineEvent` is now a flush message (no border/box); the **current** version is a purple-tinted highlight **band** + "current" pill; the AI is a **dark rounded square + amber sparkle** (people stay circles); "see changes ▸" moved top-right. **Comments now show "✓ Addressed in vN →"** — derived from the version timeline (a comment in a past round was responded to by the NEXT version; `buildVersionSpine` sets it; no schema change — comments still have no `resolved_at`). The version sub-line counts **comments** too ("addressed 1 comment, 1 request"). Added a "▾ Feed opens here · since vN" marker on the current round. The item-card resolution tag ("✓ Addressed in vN →" / "Won't action") moved to **its own line below the content** (matches the reference).
- **Files:** `viewer/VersionSpineEvent.tsx`, `viewer/feed-items.ts` (implicit comment addressing), `viewer/DeckFeed.tsx` (comments in the summary + "feed opens here"), `viewer/FeedItemCard.tsx` (tag below content); docs `design-system.md`, this tracker.
- **Verified by:** `tsc`+`eslint` clean. Preview: version events render as messages (border 0 / transparent), current = band `#f1eff9` + AI dark square (8px radius); comments struck "Addressed in v2/v3 →"; sub-lines "addressed 1 comment, 1 request/removal"; "FEED OPENS HERE · SINCE V3"; "Won't action". No console errors. (Screenshot blocked — backgrounded tab; DOM/computed-style verified.)
- **Flags:** schema? **no** (this pass). security? no. MCP? no. **Not committed.**

### 2026-06-17 — P1.2: version-spine architecture + AI provenance
- **Items touched:** P1.2 (still 🔵). Restructured the feed so VERSIONS are the backbone (spine), not plain text lines.
- **Spine + rounds:** new pure `buildVersionSpine` (feed-items.ts) groups each version + the conversation that happened during it (by created_at interval). New `VersionSpineEvent.tsx` (ONE component): **v1** = rich "[Owner] started this huddle · title · N slides"; **v2+** = lean "✦ [AI] published vN · N slides" + "requested by [name] · addressed N requests[, N removals]" + "see changes ▸" (a plain list of resolved items, NOT an AI summary). Conversation **indents** under each spine event with a thread line; current version gets a "Current" pill + tint; the feed **opens scrolled to the current version** with an "↑ earlier in this huddle" affordance (verified scrollTop 1625/2511). New `LazyThumbnailStrip.tsx` renders each version's slides as IntersectionObserver-mounted iframes (6 live, not ~30).
- **AI provenance (SCHEMA + MCP/extension capture):** new nullable `deck_versions.source` column (migration `docs/deck-versions-source-migration.sql` — **founder must run it in prod**). `snapshotVersion`/`storeSlides`/`updateDeck` thread `source`; `getDeckVersions` selects it. Extension path → `"claude"`; MCP path → derived from the OAuth client (`parseClientId` clientName/redirect URIs → "claude"/"chatgpt", else null → generic "AI", never guessed). Existing versions have no source → shown as generic "AI".
- **Addressed / dismissed:** the loaders now optionally include RESOLVED stubs/flags (`includeResolved`; `resolved_at` added to the row types + selects, graceful pre-migration fallback). The feed maps `resolved_at` → the version that addressed it and shows the item struck "✓ Addressed in vN" (links to that spine event); owner-dismissed → "Won't action". (Comments have no resolution column → only "Won't action" applies to them.)
- **Files:** NEW `viewer/VersionSpineEvent.tsx`, `viewer/LazyThumbnailStrip.tsx`, `docs/deck-versions-source-migration.sql`; MODIFIED `lib/slide-store.ts` (source + resolved loaders + getDeckVersions), `app/mcp/route.ts` + `app/api/slides/route.ts` (capture source), `viewer/feed-items.ts` (buildVersionSpine), `viewer/DeckFeed.tsx` (spine layout + scroll + per-version HTML), `viewer/page.tsx` (load resolved + per-version HTML), `viewer/FeedItemCard.tsx` (addressed/dismissed tags); docs `design-system.md`, this tracker.
- **Verified by:** `tsc`+`eslint` clean. Preview of the real `DeckFeed` (mock: 3 versions Greg/Claude/ChatGPT, resolved + dismissed items): spine lines + Current pill + "Claude/ChatGPT published vN" + "addressed 1 request/removal" + "✓ Addressed in v2/v3" + "Won't action"; 7 cards (no dups); opens at current; narrow (420) = cards stack, indent 12px, peek hidden. Deck viewer re-checked unregressed. No console errors. (Screenshot blocked — backgrounded preview tab; DOM/computed-style verified.)
- **Flags:** schema change? **YES** (`deck_versions.source` — migration to run). security-relevant? **YES** — provenance capture on the MCP + extension WRITE paths (no new permissions; reuses existing auth); resolved-item loading reuses the same access/redaction. MCP surface? **YES** (capture only — no new tools/params exposed to the AI). **Not committed.**

**Phase-1 completeness check (P1.2) — yes/no:**
- All item types in the stream (comments + stubs + flags + version events)? **YES** — `buildVersionSpine` composes all four (versions as the spine; the rest as rounds).
- Instrumented for feed-vs-deck, deck view too? **YES (seam)** — `deck_landing_viewed{view}` on both feed + deck, `feedback_added` on the deck hooks; **but silent until a PostHog key is set (P0.2)**.
- Flag-gated, default-ON for partners, others land on the deck? **YES** — `FEED_PARTNER_EMAILS` + `?view=feed`/`?view=deck`.
- Stays read-only (no Phase-3 leakage)? **YES** — no composer/threading/decisions/quoting; "see changes" is a read-only list.
- Participation round-trips (read → open deck → comment → back in feed)? **YES** — "Open deck"/"Open slide N" → existing controls; new items load into the feed.

**Outstanding before the Phase-1 gate (report only, not fixing now):**
- **P0.2 analytics key** not set → all events silent until founder adds `NEXT_PUBLIC_POSTHOG_KEY` (and runs nothing else). Gate evidence needs this on.
- **Run the `deck_versions.source` migration** in prod so NEW versions capture provenance (old ones stay generic "AI").
- **Feed not committed/deployed**; founder prod smoke-check pending.
- **Mobile pass** still owed: the feed is desktop-first (deck peek hides < lg, no bottom sheet); P1.1 **Stage E (mobile)** for the deck viewer is parked.
- **P1.1 Stage F** (rollout): floating viewer is default-for-all; per-account viewer targeting parked → P2.
- **P1.3 viral loop v0** (viewer badge · recipient CTA · claim funnel · export footer): not started.
- Minor: feed item-card thumbnails use the CURRENT deck's slide at that index for comments on older versions (the spine strips ARE version-accurate); profiles/display-names are Phase 2 (names derive from email today).

### 2026-06-17 — P1.2: avatar refinements (founder polish round)
- **Items touched:** P1.2 (still 🔵) — four founder-requested avatar tweaks, all in the shared `Avatar` system: (1) **collaborator avatars are now OUTLINE ONLY** — transparent fill + 2px coloured ring + coloured initials (the light tint is gone). (2) **New person palette** steered away from the system colours — blue `#2563EB`, pink `#DB2777`, coral `#EA580C`, slate `#475569`, rose `#BE123C`, brown `#92400E`: **no purple** (brand/buttons), **no teal/green** (comments), **no amber** (the AI). The **owner is now FILLED in their person colour** (white initials) rather than purple, so avatars never read as a button/chip/AI. (3) **`(you)`** tag next to the signed-in viewer's name in feed cards. (4) **`(owner)`** tag next to the deck owner's name; combined when both, e.g. "Greg (you · owner)".
- **Files:** `viewer/Avatar.tsx` (palette + outline-only + owner-filled-person-colour), `viewer/FeedItemCard.tsx` (+`currentUserId`, the you/owner tags), `viewer/DeckFeed.tsx` (pass `currentUserId`); docs `design-system.md` §5, this tracker.
- **Verified by:** `tsc`+`eslint` clean. Browser preview (viewing as owner greg@getpinpoint): owner avatar = filled **blue** (person colour, no purple) in BOTH the account menu and the feed; collaborators = transparent outline (pink, coral); no purple/teal/green avatar anywhere; the owner's card reads "Greg **(you · owner)**". No console errors.
- **Flags:** schema? no. security? no. MCP? no. **Not committed.**

### 2026-06-17 — P1.2: avatar owner role-shape centralised (surgical fix)
- **Items touched:** P1.2 (still 🔵) — surgical fix to the avatar role SHAPE (filled vs outline); colours/initials unchanged. **Root cause:** the filled-vs-outline decision was computed in TWO places (the feed card did `who.userId === deckOwnerId`; the Huddlers cluster used `participant.isOwner`), so the surfaces could diverge.
- **Fix:** the owner decision now lives in ONE place — `viewer/Avatar.tsx`. The component takes `userId` + `ownerId` (the deck's `user_id`) and decides `isOwner = userId === ownerId` itself → owner = FILLED purple, everyone else = OUTLINE (tint + ring), AI = ink+sparkle. Removed the per-surface owner logic: `FeedItemCard` and `HuddleAvatars` just pass `userId`+`ownerId` now. Threaded `deckOwnerId` (= `decks.user_id`) into the cluster on BOTH surfaces — `DeckFeed` already had it; added a `deckOwnerId` prop to `FloatingViewer` (from `page.tsx`).
- **Owner the code resolves (for founder to confirm):** the multi-account test deck "SlideHuddle — Competitive Benchmark" (`c0e8f602`) is owned by **greg@getpinpoint.com** (`decks.user_id`); greg.manzanera + jpcastrog (Juan Castro) are collaborators. (If that's not the expected owner, the deck's ownership is a *data* issue, not the avatar.)
- **Scope note:** `CommentsPanel`'s author avatar (deck-viewer panel) is a separate generic, non-owner-aware circle — left as-is (not part of this bug); `AvatarMenu` is the account chrome — left as-is. Both flagged as optional follow-ups to unify onto the shared `Avatar`.
- **Files:** `viewer/Avatar.tsx`, `viewer/HuddleAvatars.tsx`, `viewer/FeedItemCard.tsx`, `viewer/FloatingViewer.tsx`, `viewer/page.tsx`; docs this tracker.
- **Two follow-ups same session (founder testing on the real deck):** (1) **Account avatar unified** — the top-right `AvatarMenu` was a separate generic lavender "G" chip, so the OWNER looked different there vs the feed. It now renders the shared owner-aware `<Avatar>` when given deck context (`userId`+`ownerId`, passed from `DeckFeed`+`FloatingViewer`; the global `TopNav` still gets the simple chip). Verified: signed-in-as-owner, the account avatar = **GR filled purple**, identical to the feed; cluster shows the two collaborators outlined. Files: `components/AvatarMenu.tsx`, `viewer/DeckFeed.tsx`, `viewer/FloatingViewer.tsx`. (2) **Version-line copy bug** — read "Deck **deck** v1" with no actor; fixed to "Deck v1 shared · 4 slides" / "Greg published deck v2 · 3 slides". File: `viewer/DeckFeed.tsx`.
- **Verified by:** `tsc`+`eslint` clean. Browser preview of the real `DeckFeed` mirroring the deck (owner greg@getpinpoint, collaborators greg.manzanera + Juan Castro), viewed AS a collaborator so the owner appears in the cluster. Computed-style check: **owner (GR) = FILLED purple in BOTH the Huddlers cluster AND the feed cards**; collaborators (JP blue, GM pink) = OUTLINE in both; the ONLY filled avatar anywhere on the page = the owner. Deck viewer (`/viewer`) re-checked — unregressed, no console errors. (Screenshot still blocked by the backgrounded preview tab → DOM/computed-style verified.)
- **Flags:** schema? **no.** security-relevant? **no.** MCP? **no.** **Not committed.**
- **Recommended next:** founder confirm greg@getpinpoint is the expected owner of that deck; remove the throwaway `/feed-preview` route; then commit/deploy.

### 2026-06-16 — P1.2: feed information-design pass (avatars, horizontal cards, thumbnails, click-to-peek)
- **Items touched:** P1.2 (still 🔵) — a styling/structure pass on the feed (no new data, no new features; none of the Phase-3 set). Built per founder brief.
- **Avatar system (`viewer/Avatar.tsx`, new):** ONE reusable component, two signals — **shape = role** (owner FILLED purple / collaborator OUTLINE: coloured ring + initials on the colour's light tint / AI ink circle + amber sparkle) and **colour = person** (deterministic hash of user id → 6-colour palette; purple reserved for owner, amber for AI). Initials from display name (first+last) or email local part (strip trailing digits, split on `._-+`). `HuddleAvatars` refactored to use it, so the deck viewer's top-bar cluster gets per-person colours too.
- **Horizontal card (`viewer/FeedItemCard.tsx`, new):** thumbnail left · content middle · slide pill top-right; type-aware (comment/stub/flag) with distinct icon + thumbnail. **Thumbnails — cheap path:** scaled-iframe live render of the slide HTML (reused `FloatingThumbnailStrip`'s `SlideThumb` pattern + `buildSrcdoc`); comment = clean real slide, flag = real slide greyed + X overlay (red card border), stub = a dashed-teal mini-slide rendered from its 3 inputs. Pills type-aware ("Slide N" vs "After slide N ↓"). Narrow screens: thumbnail stacks above text.
- **`DeckFeed` rebuilt:** click any item → selects it (purple ring) + moves the deck peek to its slide; **peek shows per-slide stats** for the selected slide (always "N comments"; "N flagged for removal" in red and "N requested here" only when > 0 — aggregated via `useMemo` over the FULL comment/flag/stub arrays, not the rendered subset). **Version timeline lines** ("Greg shared deck v1 · 2 slides") give the stream shape; day dividers kept. **"Open slide N"** deep-links via a new `?slide=N` param (`page.tsx` → `FloatingViewer` maps it to the matching display-item on mount).
- **Data-bug finding:** queried every deck's rows directly — **no duplicate comments/stubs/flags exist anywhere**; the feed renders each row once. The "duplication" was almost certainly the two near-identical grey version lines (now distinct story lines). Hardened anyway: `feed-items.ts` dedupes by key + collapses duplicate version numbers; each card field renders exactly once. Asked founder to point at a specific deck if a real duplicate persists.
- **Files:** NEW `Avatar.tsx`, `FeedItemCard.tsx`; MODIFIED `DeckFeed.tsx`, `feed-items.ts`, `HuddleAvatars.tsx`, `FloatingViewer.tsx` + `page.tsx` (`?slide=N`); docs `design-system.md` §5 (Avatar + Feed-item-card entries), this tracker.
- **Verified by:** `tsc` + `eslint` clean. Browser preview of the real `DeckFeed` (mock route, owner + 3 named collaborators, all three item types, 3 versions): **avatars distinct + stable** (Greg = filled purple owner; Aisha pink, Jordan blue, Sara teal outlines), 6 cards / 6 iframes, type-aware pills ("After slide 2" for the stub), **click-to-peek** jumps the peek + updates per-slide stats (stub → "1 requested here"; flag → "1 flagged for removal" red, "0 comments"), `&slide=N` deep-link updates, version lines read as a story. Responsive checked at 1280 (card row, peek shown) and 420 (card column = thumbnail ABOVE text, peek hidden). No console errors. **No screenshot image** — the preview tool's tab is backgrounded (`visibilityState:hidden`), which suspends the screenshot compositor; verified via DOM/computed-style instead. Throwaway `/feed-preview` route kept temporarily so the founder can view the multi-user design live.
- **Flags:** schema? **no.** security-relevant? **no** — presentation only; same redaction rules; no auth/RLS/service-role/MCP. MCP surface? **no.** **Not committed.**
- **Recommended next session:** founder views `/feed-preview` (or adds 2–3 named test users to a deck) → confirm avatars differ per person; remove the throwaway route; then commit/deploy.

### 2026-06-16 — P1.2: read-only conversation feed (alternative landing) + analytics seam
- **Items touched:** P1.2 ⬜→🔵 — built the read-only "Huddle feed" as an alternative LANDING surface for a deck (design-system §6.2), composing only data we already store. Founder decisions this session (via AskUserQuestion): **install PostHog now**; gate via an **email allowlist + `?view=` override**; **oldest-first** ordering.
- **What it does:** `feed-items.ts` (pure) merges deck-version events + all-version comments + requested slides (stubs) + removal flags into one oldest-first stream. `DeckFeed.tsx` renders it as chat-style cards (avatar + author + relative time + body; "Slide N · vN" chips that re-point the peek; stub & flag cards; dismissed comments struck), with day dividers and a catch-up ribbon, and the deck **demoted to a right "peek"** (a scaled live iframe via the shared `parseDeck`/`buildSrcdoc`) + an **Open deck** button (carries `?view=deck`). It is **read-only** — no composer; you participate by opening the deck and using the existing controls, and your feedback shows back up in the feed.
- **Gating:** `page.tsx` resolves a `landing: feed|deck` for stored decks — `?view=feed`/`?view=deck` win per-URL; otherwise a signed-in viewer whose email is in `FEED_PARTNER_EMAILS` lands on the feed, everyone else on the deck. New read-only `getAllCommentsForDeck` (all versions; same access check + email re-resolution as the version-scoped loader). Deck-path round-trips unchanged when not showing the feed.
- **Instrumentation (P0.2 seam):** added `posthog-js` + `lib/analytics.ts` (`track`/`identifyUser`, **safe no-op until `NEXT_PUBLIC_POSTHOG_KEY` is set**) + a root `PostHogProvider`. Events: `deck_landing_viewed{view:feed|deck, role, isPartner, counts}` on BOTH landings (the "which view do partners use" evidence) · `feed_open_deck` · `feedback_added{kind}` in the 3 deck hooks (the "did feedback volume go up" evidence). **No PostHog account/key yet — Greg owns that decision (region/account); measurement turns on the moment a key is set, no code change.**
- **Reuse:** extracted the anon `ReviewingChip`/`SharedDeckChip` to shared `HuddleChips.tsx` (used by both viewer + feed); extracted `formatRelativeTime` to `lib/relative-time.ts` (CommentsPanel now imports it); reused `DeckVersionNav`/`HuddleAvatars`/`AvatarMenu`/`parseDeck`/`buildSrcdoc`/`computeArrivalActivity` + the existing redaction rules.
- **Files changed:** NEW `web/src/lib/analytics.ts`, `web/src/lib/relative-time.ts`, `web/src/app/PostHogProvider.tsx`, `web/src/app/viewer/feed-items.ts`, `web/src/app/viewer/DeckFeed.tsx`, `web/src/app/viewer/HuddleChips.tsx`, `web/.env.example`; MODIFIED `web/src/app/layout.tsx`, `web/src/lib/slide-store.ts` (getAllCommentsForDeck), `web/src/app/viewer/page.tsx` (gating + feed branch), `FloatingViewer.tsx` (landing event + chip import + `isPartner`), `useDeckComments.ts`/`useDeckStubs.ts`/`useDeckFlags.ts` (feedback_added), `CommentsPanel.tsx` (shared relative-time); `web/package.json` (+posthog-js). Docs: this tracker.
- **Verified by:** `npm run typecheck` + `npm run lint` clean (analytics no-ops without a key). Real-module node test of `buildFeedItems` (oldest-first, all 4 kinds, version-before-feedback tie). Browser preview of the real `DeckFeed` with mock data: all four card kinds render in correct order with day dividers + catch-up ribbon; dismissed comment is `line-through` + "Won't send to AI"; peek iframe scales 288/1280 = 0.225; **Open deck href = `/viewer?id=…&view=deck`**; `deck_landing_viewed` fires; no console errors. Deck viewer (`/viewer` sample) re-checked — renders unregressed (floating controls + iframe + counter), its own `deck_landing_viewed` fires. Throwaway preview route + test file deleted. **No screenshot** — preview tab backgrounded (compositor blocked), DOM-snapshot-verified instead.
- **Flags:** schema change? **no.** security-relevant? new read-only `getAllCommentsForDeck` reuses the **same** owns-deck-OR-shared access check + trustworthy author-email re-resolution as the existing loader; anonymous viewers still get `[]` comments and email-redacted stubs/flags (unchanged rules). PostHog is **client-side** analytics gated on a public env key; identify only fires for signed-in users. No auth/RLS/service-role/MCP changes. MCP surface? **no.** **Not committed.**
- **New parking-lot entries:** P0.2 now partially seeded (PostHog lib + seam + events); feed gate is an env allowlist (per-account toggle → P2 workspaces); mobile feed layout deferred to the mobile pass.
- **Recommended next session:** founder end-to-end test (add your email to `FEED_PARTNER_EMAILS` in `web/.env.local`, open a deck with comments → land on the feed → Open deck → comment → see it in the feed); set a PostHog key to light up measurement; then commit/deploy. After partners use it, P1.2 → ✅ on the Gate-G1 evidence.

### 2026-06-14 — Stage F (rollout): position + the logical next step (documentation only)
- **Stage F is effectively satisfied already.** The floating viewer is the default for everyone (deployed today, CI run 27511906445 green), with `?view=classic` + the `FLOATING_VIEWER_DEFAULT` env kill switch as fallbacks. At this stage the only real audience is the close design-partner group, so "everyone" ≈ "the test group" — no per-account/test-group targeting is needed yet.
- **Most logical next step to CLOSE Stage F:** a **founder production smoke-check (👤)** — open the live deck viewer, confirm the redesign renders correctly, and confirm `?view=classic` still falls back to the old viewer. That's the "safely" half (CI/type-check is already green). After that, Stage F can be marked done.
- **When the founder will need to address per-account/test-group targeting:** defer it until **Phase 2**. It becomes *needed* only when the audience broadens beyond the close partner group — i.e., when there are users you'd want to keep on the classic viewer while partners get the new one — OR when Phase 2's **workspaces + profiles/per-user flags** land (the natural home for an account-level toggle). Logged in the Parking Lot (proposed P2). Until then, default-for-all is the correct, lightest choice.
- **Related dependency for *measuring* rollout:** analytics (**P0.2**, deferred) — needed to see whether partners actually use the new viewer; relevant as design-partner recruitment (**P1.4**) ramps.
- **No code changes** — documentation only.

### 2026-06-14 — Committed + pushed today's P1.1 desktop work to `main`
- Committed the day's P1.1 desktop work as one commit and pushed to `main`: the colour-rule/cluster refinement, Stage B (inset + persistence + gear), the toast top-layer + popover-clamp fixes, Stage C (version-awareness + flag UI + orphan nudge), Stage D ("N reviewing"), and the flag/requested-slide "…" → bottom-right + dismiss-hide.
- **Deploys to production via Vercel** (push-to-main). The redesigned floating viewer (now improved) goes live; the classic viewer (`?view=classic`) + the `FLOATING_VIEWER_DEFAULT` kill switch remain as fallbacks.
- **Files:** the modified + new viewer/component files from the stage entries below; docs (`design-system.md`, this tracker). `.wip-backup/` deliberately NOT committed (pre-existing local backup).
- **Flags:** schema? **no.** security-relevant? the flag-creation write reuses the existing RLS `slide_flags` insert/delete; the anonymous "N reviewing" **count-only** exposure was founder-approved — both already detailed in the stage entries. No auth/service-role/MCP changes. MCP surface? **no.**
- **Verified by:** `tsc` + `eslint` clean; behaviours eval-verified through the session (no screenshots — preview tab backgrounded). CI (lint + type-check) runs on the push.
- **Next:** Stage F (rollout decision) + any desktop polish; Stage E (mobile) still parked.

### 2026-06-14 — Decision: defer Stage E (mobile) until the desktop base is complete
- **Founder decision:** the mobile pass (Stage E — floating panels → bottom sheets, hover→tap controls, thumb-reach composer) **waits** until we have a complete base **desktop** experience. Rationale: don't polish mobile against a still-moving desktop target. Stage E is **parked, not dropped**.
- **Where that leaves P1.1:** desktop Stages B/C/D done; remaining = any desktop polish + **Stage F** (rollout). Note: the floating viewer is *already* the default in production (committed earlier, 0b1bea3); the B/C/D improvements are all **uncommitted/undeployed** (bundled) — so "complete base desktop experience for real users" = commit + deploy that work.
- **Next:** founder to direct — continue desktop polish, or do Stage F (commit/deploy the desktop work + decide default-for-all vs test-group-only).

### 2026-06-14 — P1.1 Stage D: recipient (guest) view — "N reviewing" copy + z-index/inset confirmed
- **Items touched:** P1.1 (still 🔵). Stage D's three bullets:
  - **"N reviewing" guest copy (§10.6)** — anonymous link viewers now get a count-only **"N reviewing"** chip instead of the generic "Shared deck" (and never the team word "Huddlers"). Signed-in viewers still get the `HuddleAvatars` "N Huddlers" cluster. New `ReviewingChip` in `FloatingViewer`; `page.tsx` computes the participant **count** for every floating stored-deck view and passes `reviewingCount` (identities still gated to signed-in via `participants`).
  - **Z-index glitch (§10.3)** — confirmed **already fixed** by the rewrite: the deck title renders *inside* the brand pill (verified `titleInsideBrandPill: true`, z-index auto), so the old "title behind the pill" glitch can't occur. No change needed.
  - **Inset + Floating-Canvas styling** — the recipient view is the same `FloatingViewer`, so the Stage-B inset and styling already apply. No change needed.
- **Files changed:** `web/src/app/viewer/FloatingViewer.tsx` (`ReviewingChip` + `reviewingCount` prop + anon branch); `web/src/app/viewer/page.tsx` (compute `reviewingCount`, pass it). Docs: this tracker.
- **Flags:** schema change? **no.** security-relevant? **YES (intentional, founder-approved):** anonymous viewers now receive a participant **COUNT** (e.g. "3 reviewing") — previously they got zero participant info. It is a count only; **no names or emails** reach an anonymous viewer (verified: rows are gated behind `canSeeCollaboratorEmails`). No auth/RLS/service-role/MCP changes. MCP surface? **no.** Minor perf note: this adds one `getDeckParticipants` query per anonymous stored-deck view (was signed-in-only). **Not committed.**
- **Scope note:** the *full* client SURFACE from mockup Screen 5 (agency logo leading, "Reviewed on SlideHuddle" footer, a Decisions-log tab, named-guest identities) is **Phase 2/5** (named guests + Studio branding), not P1.1. Stage D delivered the three bullets in the brief.
- **Verified by:** `tsc`+`eslint` clean. Mock anonymous-recipient render (eval): chip reads "3 reviewing", no "Shared deck"/"Huddler" text, title inside the brand pill. No screenshot (preview tab backgrounded this session).
- **Recommended next session:** Stage E — mobile bottom sheets (its own session per the plan); then Stage F (rollout).

### 2026-06-14 — P1.1 Stage C: version awareness + flag-for-removal UI + orphan nudge
- **Items touched:** P1.1 (still 🔵). Stage B approved by founder. Built Stage C's three functional gaps in the floating viewer (zoom was already removed):
  - **Version awareness** — new `useDeckVersionWatch` hook (a faithful mirror of SlideViewer's 12s `/api/deck-version` poll, so the live viewer stays untouched) surfaces an amber **"This deck was revised — Load v{n}"** prompt when the deck is revised out-of-band (e.g. the AI publishing via MCP). It **prompts** (never auto-refreshes, so a half-typed comment isn't lost); "Load vN" → `router.refresh()`, and a new version **`key`** on the floating viewer (page.tsx) remounts it with the new slides/comments. The key also fixes a latent bug where switching versions via the chip kept stale comments.
  - **Flag-for-removal UI** — reused the classic viewer's `SlideFlagControl` ("…" menu, **bottom-right** of the slide in the floating viewer via a new `position` prop; classic stays top-left) + a new `useDeckFlags` hook (mirrors `useDeckStubs`: RLS insert/delete via the browser client; owner dismiss via `setFlagCurationAction`). Flags show inline in the comments panel and feed the live "Send to AI" prompt. **Owner-dismiss now HIDES the flag** (founder request: a dismissed flag was lingering struck-through and still "felt in effect") — the floating viewer surfaces only non-dismissed flags, so the slide reads un-flagged; the row stays in the DB for audit + out of the AI prompt, and the slide can be re-flagged. For consistency, the **requested-slide (stub) "…" edit/delete menu** was also moved to the same bottom-right corner with the same dark hover-revealed look (a `placement` prop on `StubSlideView`/`StubActionsMenu`; classic viewer stays inline).
  - **Orphan-deck nudge** — ported the P0.4 fix: on an unclaimed deck the panel shows "ask the creator to claim it" instead of a composer, and comment/flag/stub creation + the flag "…" are gated off. Resolves the parking-lot item.
- **Files changed:** NEW `web/src/app/viewer/useDeckVersionWatch.ts`, `useDeckFlags.ts`; `FloatingViewer.tsx` (wire all three + the version banner + `SlideFlagControl` on the slide); `page.tsx` (pass `isOrphanDeck` + `viewerFlags` to the floating viewer; add the version `key`). Docs: this tracker.
- **Flags:** schema change? **no.** security-relevant? **flag creation is a collaboration WRITE** — but it reuses the *same* RLS-governed `slide_flags` insert/delete the classic viewer already does (no new permission surface). Data note: the floating viewer now receives `viewerFlags` for ALL signed-in collaborators (was owner-only) — this is **parity with the classic viewer**, which already passes `viewerFlags`; anonymous viewers' `flagged_by` emails stay redacted. No auth/service-role/MCP changes. MCP surface? **no.** **Not committed.**
- **Verified by:** `tsc`+`eslint` clean. Mock-props render (eval): owner with a flagged slide → "…" flag control present + the panel shows "Flagged for removal" + the reason + a flagged pill; orphan variant → panel shows the claim nudge, composer hidden, flag "…" hidden. The version poll/banner is a line-for-line mirror of the in-prod classic-viewer poll (typecheck-clean; live-triggering needs a real out-of-band revision + ~12s, so not forced in eval). No screenshot (preview tab backgrounded this session).
- **Recommended next session:** Stage D — "3 reviewing" guest copy on the recipient surface (count-only to anon, per founder) + re-verify the recipient z-index; then Stage E (mobile).

### 2026-06-14 — Two viewer bug-fixes (founder-spotted in Stage-B screenshots): toasts top-layer + popovers clamp on-screen
- **Items touched:** not formal P-items — two bugs the founder caught. **(1) Toasts were hidden.** The "Link copied" / "Sent to AI" confirmation toasts rendered *inside* the `z-20` backdrop-blur pill cluster, so the `z-30` comments panel painted over them (a `z-…` bump can't escape an ancestor stacking context). **(2) "Request a slide" form ran off-screen.** `PortalPopover` only flipped up when a panel fully fit above; a tall form opened from a *low* thumbnail fit neither way, opened downward, and spilled under the viewport with its submit button unreachable.
- **Fixes:** (1) new shared `AnchoredToast` portals toasts to `<body>` at `z-10000` (above every panel/pill/popover) — **fixed rule**, now codified in design-system §3.2. (2) `PortalPopover` clamps the panel's `top` so the whole panel always stays on-screen (shifts up to fit; may overlap the anchor, far better than an off-screen button).
- **Files changed:** NEW `web/src/components/AnchoredToast.tsx`; `web/src/components/PortalPopover.tsx` (vertical clamp); `web/src/app/viewer/CopyLinkButton.tsx` + `SendToClaudeButton.tsx` (route toasts through `AnchoredToast`). Docs: `design-system.md` §3.2, this tracker.
- **Scope note:** `AnchoredToast` + `PortalPopover` are shared with the **classic** viewer too, so both fixes apply there as well (toasts still appear under their button — just on the top layer now).
- **Verified by:** `tsc`+`eslint` clean. Temp-page eval: toast wrapper is a direct child of `<body>`, `position:fixed`, `z-index 10000`, 6px below its anchor; a 480px popover anchored at the bottom edge (innerHeight 578) clamped to top 90 / bottom 570 → fully on-screen. Temp page deleted.
- **Flags:** schema? **no.** security-relevant? **no** (presentation only; no auth/RLS/service-role/MCP). MCP surface? **no.** **Not committed.**
- **Recommended next:** still holding for founder visual sign-off of Stage B (sliver + counter + cluster + gear + these fixes); then Stage C.

### 2026-06-14 — P1.1 Stage B: persistence policy (rail sliver + always-on counter)
- **Items touched:** P1.1 (still 🔵) — implemented the §4.1 persistence policy on the floating viewer, completing **Stage B (deck-viewer redesign)** of the founder's A–F brief (inset + action-cluster were already done). The rail no longer fully disappears when closed: it collapses to a **sliver** on the left edge — a 14px rounded strip showing a teal **comment-activity dot per slide that has comments** ("the team's fingerprints", §3.2). Hover, tap, or press **`T`** expands it to the full `FloatingThumbnailStrip`. The **slide counter is now always visible** (§4.1) instead of fading on idle. `T` is guarded so it doesn't fire while typing in the comment composer (arrow-key behaviour left unchanged, per founder). The bottom-left **pin** was replaced by a **gear → settings dropdown** (opens upward via `PortalPopover`) holding a "Pin floating bars" toggle (reuses the existing pin state; a purple dot on the gear shows when it's on).
- **Reuse:** built on the existing components — the sliver reuses `commentCountBySlide` (already computed for the thumbnail badges) and opens the existing `FloatingThumbnailStrip`; no rewrite. `useDeckComments`/`useDeckStubs` untouched.
- **Files changed:** `web/src/app/viewer/FloatingViewer.tsx` only (sliver JSX + `commentedSlides` memo; counter de-faded; `T`-toggle effect (typing-guarded) placed after `reveal`; left nav-arrow nudged clear of the sliver; bottom-left pin → gear/settings dropdown via `PortalPopover`; header comment). Docs: this tracker.
- **Verified by:** `npm run typecheck` + `npm run lint` exit 0. Rendered the real `FloatingViewer` with mock props (5 slides; comments on slides 1/3/4; signed-in owner) on a throwaway dev route and inspected the DOM: **sliver present** at x=8, w=14, full-height; **exactly 3 teal dots** (matching the 3 commented slides); **counter "1 / 5" at opacity 1**; dispatching a `T` keydown **swaps sliver → full 185px strip** and the counter stays. Temp route deleted. Gear→settings verified on the sample deck: gear present, menu opens with the "Pin floating bars" toggle, toggling flips aria-checked false→true and shows the purple pinned-dot. **No screenshot image** — the preview tab is backgrounded this session (`visibilityState: hidden`), which blocks capture; founder to eyeball via self-view steps (in response).
- **Flags:** schema change? **no.** security-relevant? **no** — presentation-only; no auth/RLS/service-role/MCP; no DB writes. MCP surface changed? **no.** **Not committed.**
- **Held for approval:** per the founder's brief, STOP after Stage B for a visual sign-off before Stage C. Awaiting that.
- **Recommended next session:** on approval, Stage C — port the classic viewer's version-poll into a shared hook for the floating viewer + add the flag-for-removal UI (reusing `SlideFlagControl`).

### 2026-06-14 — P1.1 Stage 1: top-right action cluster + colour-rule refinement
- **Items touched:** P1.1 (still 🔵) — Stage 1 cosmetic pass on the floating viewer's top-right action cluster. **The plan changed mid-stage:** I first made Send to AI amber (design-system §10 #2 "amber restored"), then the founder refined the colour rule and reversed it. Final state, hierarchy-by-weight not colour: **Share** = filled purple (the only filled button — everyday primary action); **Send to AI** = purple-outline split (reverted to the original); **Comments** = a quiet bare **teal icon + count** (was a filled green pill — it only toggles the panel, so it's the lightest control). Order left→right Send to AI · Comments · Share (already correct). Recipient-view z-index (#3) and brand-pill icon (#4) confirmed already clean in the current rewrite (no change needed).
- **The colour rule (now canonical):** **purple = actions you take** (Share, the composer's Send, **and Send to AI** — invoking the AI is something you do); **amber = the AI's own voice** (its feed posts, "Queued/Sent to AI" chips, its avatar) — *never* the button that invokes it; **teal = the team** (unchanged). This supersedes design-system §2.2 / §10 #2.
- **Files changed:** `web/src/app/viewer/SendToClaudeButton.tsx` (reverted to purple-only — removed the amber `tone` option I'd added), `web/src/app/viewer/FloatingViewer.tsx` (dropped `tone="amber"`; restyled the Comments toggle to a bare teal icon+count), `web/src/app/viewer/ThumbnailStrip.tsx` (fixed a stale "amber" comment — the classic button is actually purple). Docs: `design-system.md` (§2.2 colour table + rules, §3.2 cluster grammar, §5 Send-to-AI + Comments specs, §6 Claude-queue send, §10 #2 marked superseded, header → v2.1 + mockup-lag note); this tracker.
- **Verified by:** `npm run typecheck` + `npm run lint` both exit 0. Computed-style spot-check confirmed Send to AI back to purple-outline (the Stage-1 amber was earlier verified at `#C77D11`, now reverted). **No fresh screenshot** of the full cluster: it only renders for a signed-in owner with feedback, and this session's preview tab is backgrounded (`document.visibilityState === "hidden"`), which blocks the screenshot compositor (page still responds to eval). Founder can self-view — see response.
- **Flags:** schema change? **no.** security-relevant? **no** — presentation-only; no auth/RLS/service-role/MCP touched (no DB writes this stage either). MCP surface changed? **no.** **Not committed.**
- **New parking-lot entries:** (1) `mockups-v2.html` still shows amber Send-to-AI + filled Comments — refresh to match the v2.1 colour rule (proposed P1, low priority). (2) `ArrivalBanner.tsx` styles team-comment arrivals in **amber**, but the new rule makes team = teal — revisit (proposed P1/P3).
- **Recommended next session:** Stage 3 — rail sliver + always-on counter (design-system §4.1/§5), replacing the current "collapse everything on idle". Then Stage 4 (version poll + flag UI + orphan-nudge port).

### 2026-06-14 — Phase 1 begins · P1.1 Stage 2: occlusion fix (inset, not overlay) + zoom removed
- **Items touched:** P1.1 ⬜→🔵 — implemented the design system's §3.3 **"inset, not overlay"** must-fix (design-review punch-list #1) in the floating viewer: when the thumbnail strip and/or comments panel is open, the slide now **scales down and shifts into the safe area beside the panel** instead of rendering underneath it. Also **removed the inert zoom placeholder** (founder decision this session: remove, not implement). This is Stage 2 of the agreed P1.1 plan; founder chose to start here.
- **Files changed:** `web/src/app/viewer/FloatingViewer.tsx` only — inset layout constants (STRIP_INSET/PANEL_INSET); `leftInset`/`rightInset` gated on each panel actually rendering; `measure()` shrinks the fit-box by the insets (clamped) and re-runs on panel toggle; `translateX` offset + 200ms transition on the slide card (snaps under reduced motion); removed the `Placeholder` component + the zoom block; refreshed the header comment.
- **Verified by:** `npm run typecheck` + `npm run lint` both exit 0. Browser preview at 1280×800 on a throwaway 5-slide stored deck: strip-closed = full-bleed (slide 0→1280); strip-open = slide shrinks to 1067 and shifts to left:213, a clean **12px gap** to the strip's right edge (201) → **no overlap** (previously the panel floated over the slide). Screenshot captured; no console errors. The comments-panel side reuses the identical `rightInset`/`PANEL_INSET`/offset code path (verified by construction — a live both-panels-open shot needs a signed-in session; offered to founder).
- **Flags:** schema change? **no.** security-relevant? the **code** change is presentation-only — **no auth/RLS/service-role/MCP touched.** The **verification** created + deleted ONE throwaway orphan deck in the **production** DB via the service-role key (same authorised pattern as P0.3/P0.4); cleanup re-verified ("No slides to display." on the deleted id). Temp seed script removed. MCP surface changed? **no.** **Not committed / not deployed.**
- **Known minor cosmetic (not a bug):** with only the left strip open, the slide can touch the right viewport edge (that side has no panel, so it stays full-bleed there). Acceptable; can add a symmetric edge gap later if founder prefers.
- **New parking-lot entries:** none (the floating-viewer orphan-deck nudge port is already logged for P1, to fold into Stage 4).
- **Recommended next session:** Stage 1 (cosmetic) — amber Send-to-AI **[careful: `SendToClaudeButton` is shared with the classic viewer; parametrise the colour so classic stays purple]**, verify/fix recipient-view z-index (#3), confirm brand-pill icon (#4). Then Stage 3 (rail sliver + always-on counter). Founder: review the inset screenshot and approve before we build wider.

### 2026-06-13 — Floating viewer promoted to default (behind a kill switch)
- **Items touched:** none (not a formal P-item). Flipped the floating viewer from opt-in (`?view=floating`, default off) to the **default** viewer, per founder request. Classic viewer retained as `?view=classic` escape hatch; new `FLOATING_VIEWER_DEFAULT` env var is a server-side kill switch (`0`/`false`/`off` → roll back to classic, no code change). Founder chose env-var flag + escape hatch via AskUserQuestion.
- **Files changed:** `web/src/app/viewer/page.tsx` (added `floatingViewerDefault()` helper + `view === "floating" ? … : view === "classic" ? … : default` branch); `web/src/app/viewer/FloatingViewer.tsx` (header comment); docs: `architecture.md`, `TECHNICAL.md`, `FEATURE-INVENTORY.md`.
- **Verified by:** `npm run lint` + `npm run typecheck` both exit 0. Browser preview: `/viewer` (no param) renders the floating viewer (4 `[data-floating-control]`, zoom/counter chrome, no classic TopNav); `/viewer?view=classic` renders the classic viewer ("Viewing sample deck" banner + thumbnail strip, 0 floating controls); no console errors. **Not yet committed or deployed.**
- **Flags:** schema change? no. security-relevant? no — same server-fetched, role-gated, email-redacted props feed both viewers; presentation-only switch. MCP surface changed? no.
- **Known caveat carried in:** floating viewer has **no mobile layout** and **inert zoom** yet (FEATURE-INVENTORY E); making it the default means phones now get it. Uncommitted "huddle signals" work still sits in `.wip-backup/`.
- **New parking-lot entries:** none.
- **Recommended next session:** commit + deploy this; then prioritise the floating viewer's mobile layout before it's the default for real users at scale.

### 2026-06-13 — Session 3: P0.6 CI baseline (lint + type-check) ✅
- **Items touched:** P0.6 ⬜→✅ — minimal GitHub Actions CI (lint + TypeScript type-check) on push-to-main + PRs. Pushed to main; first CI run on GitHub = success.
- **Files changed (config):** NEW `.github/workflows/ci.yml`; `web/package.json` gained a `"typecheck": "tsc --noEmit"` script. No application code changed.
- **Decision:** `test-loop.mjs` is NOT in CI — it needs the service-role key + live DB (reads web/.env.local, hits prod), which must never be wired into CI. Documented safe path: run it later against a dedicated TEST Supabase project. (Also means the parked stale-test assertion no longer blocks CI.)
- **Verified by:** ran the exact CI commands locally — `npm run lint` (exit 0) and `npm run typecheck` (exit 0). The workflow YAML itself is unverified until it runs on GitHub.
- **Flags:** schema change? no. security-relevant? CI deliberately uses NO secrets/DB (the whole point). MCP surface changed? no.
- **Push/deploy:** Greg approved (option A). Pushed `main` (41041ac..9fff55b) → CI ran green on GitHub (verified via API: completed/success) AND Vercel deployed P0.3 + P0.4 to production. This was the session's first production deploy.
- **New parking-lot entries:** none.
- **Recommended next session:** founder track (P0.7–P0.9). When P0.2 (analytics) is picked up, add the P0.4 orphan-funnel instrumentation event then.

### 2026-06-13 — Session 2: P0.4 orphan-deck comment nudge ✅ + PDF export deprioritized
- **Items touched:** P0.4 ⬜→✅ — orphan decks (no owner yet) now show a clear "comments aren't available until the creator claims it" nudge instead of a comment box that silently fails at the DB. P0.5 marked deferred (founder decision — LLMs can generate PDFs).
- **Files changed:** `web/src/app/viewer/page.tsx` (compute `isOrphanDeck` after the claim logic + pass to SlideViewer), `web/src/app/viewer/SlideViewer.tsx` (prop + gate canComment/canInsert/canFlag off for orphans + pass to panel), `web/src/app/viewer/CommentsPanel.tsx` (prop + orphan footer state). Docs: tracker + `gap-analysis-plan.md` PDF deprioritization. **Code not yet committed at time of writing.**
- **Verified by:** `tsc --noEmit` + eslint clean; browser preview — created a throwaway orphan deck (anon POST), viewed it anonymously, opened the comments panel, confirmed the nudge renders (screenshot); throwaway deck deleted from the DB afterwards (verified gone). The signed-in-orphan path renders the same nudge branch (canComment gated false, tsc-verified).
- **Flags:** schema change? no. security-relevant? the fix touches no auth/RLS/service-role/MCP code; the verification used the service-role key to create + delete one throwaway orphan deck in the *production* DB (consistent with the earlier authorised test run), cleanup confirmed. MCP surface changed? no.
- **New parking-lot entries:** (1) floating viewer has the same orphan dead-end — port the nudge in P1.1; (2 — already logged) stale historical-version-chip test assertion.
- **Recommended next session:** P0.6 (CI on push to main) — bundle the stale-test fix so the suite is green; then the founder track (P0.7–P0.9). P0.4 instrumentation rides on P0.2 when analytics lands.

### 2026-06-13 — Session 1: P0.3 extension-update feedback resolution ✅
- **Items touched:** P0.3 🔵→✅ — wired `clearAddressedFeedback` into the extension update path (parity with MCP `update_deck`), verified end-to-end.
- **Files changed:** `web/src/app/api/slides/route.ts` (import + best-effort `clearAddressedFeedback` after `updateDeck` + `resolvedFeedbackCount` in the response); `web/scripts/test-loop.mjs` (added `restPost` + a P0.3 block that seeds a stub & flag, runs the token-authed update, asserts both `resolved_at` set + `resolvedFeedbackCount === 2`). **Not yet committed.**
- **Verified by:** `tsc --noEmit` + eslint clean; `node scripts/test-loop.mjs` against the live (production) Supabase via a local dev server → all 3 P0.3 assertions pass + full create→update→version loop passes (53 passed, 1 failed). The 1 failure is unrelated test-drift (Parking Lot). Test deck auto-cleaned from the DB (cleanup log confirmed).
- **Flags:** schema change? no. security-relevant? yes — the new call uses the existing (unchanged) service-role-backed `clearAddressedFeedback`; the verification run used the service-role key to seed/delete a throwaway orphan deck in the *production* DB (Greg authorised). No auth/RLS/MCP-surface changes. MCP surface changed? no.
- **New parking-lot entries:** stale `test-loop.mjs` assertion (historical-view version chip) — fix before P0.6 CI.
- **Recommended next session:** founder picks next P0 item (P0.4 / P0.5 / P0.6); commit the P0.3 code change; fix the stale test assertion when wiring P0.6.

### 2026-06-13 — Session 0: orientation, import reference docs, P0.1 ✅
- **Items touched:** P0.1 ⬜→✅ — Greg ran `docs/verify-rls.sql` in production Supabase. Result: all 7 tables `rls_enabled=true`; the anon-policy check returned 0 rows. RLS is live across the whole DB with nothing exposed to the logged-out role. The plan's #1 blocker, closed clean — no remediation needed.
- **Files changed:** imported 5 reference docs into `docs/` — `PROGRESS-TRACKER.md`, `gap-analysis-plan.md`, `design-system.md`, `voice-of-user.md`, `mockups-v2.html` (previously only in Greg's Downloads; verified never in git history). No code or DB changes.
- **Verified by:** Greg's two query results pasted back (7× `rls_enabled=true`; anon check empty). verify-rls.sql confirmed read-only (SELECT-only) before running.
- **Flags:** schema change? no. security-relevant? yes — RLS-live verification; verification only, no changes made. MCP surface changed? no.
- **New parking-lot entries:** none.
- **Recommended next session:** start P0.2 (analytics from zero). First decision for Greg: which analytics tool (PostHog vs Plausible vs alternative) — bring options + recommendation before installing.
